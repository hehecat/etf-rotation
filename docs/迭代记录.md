# ETF跨行业轮动策略 · 迭代记录

> 最后更新: 2026-07-21
> 目标: 找到稳健的跨行业ETF动量轮动策略; 并探索结构牛也能少输大盘的全天候变体
> 初始资金: 10万 | 佣金: 万0.5免5 | ETF无印花税

---

## 迭代总览

| 版本 | 核心改动 | 池子 | 收益 | 年化 | 回撤 | 夏普 | 结论 |
|------|---------|------|:----:|:----:|:----:|:----:|------|
| V1 | 多因子+迟滞+止损 | 同行业偏医药 | -43% | -28% | -61% | - | 失败: 无趋势过滤 |
| V2 | +趋势过滤+冷却+过热 | 同行业偏医药 | +84% | +41% | -12% | 3.5 | 趋势过滤是分水岭 |
| V3 | 因子权重对比 | 同行业偏医药 | +134% | +62% | -13% | 5.0 | 纯动量胜出 |
| V4 | 144组参数网格 | 同行业偏医药 | +209% | +90% | -13% | 7.0 | 持1只/7天最优, 但池子有过拟合 |
| V5 | 换池/换时间过拟合检验 | 多池 | 见详情 | - | - | - | 原池过拟合, 行业池-41% |
| V6 | 跨行业池重建 | 10赛道 | +141% | +65% | -20% | 3.2 | 真正可用的基线 |
| V7 | 新因子+双均线 | 20赛道 | +99% | +48% | -18% | 2.7 | 双均线更稳 |


### V11 全天候研究 (2026-07-21)

| 版本 | 核心改动 | 池子 | 收益 | 年化 | 回撤 | 夏普 | 结论 |
|------|---------|------|:----:|:----:|:----:|:----:|------|
| V11-C01 | 生产冻结对照 | 去重核心48 | +120% | +41% | -15% | 2.8 | 2024 跑输300约2pp |
| **V11-AW** | **pure_eff + top2 + h30/mh7** | 去重核心48 | +104% | +36% | **-12%** | **3.0** | **3/3年跑赢300** |
| V11-G15 | eff/mtf/m20 + top2 | 去重核心48 | +126% | +42% | -16% | 2.7 | 主题年更强, 最差超额薄 |

样本: 2023-11-27 ~ 2026-07-20, 上市≤2024-01-02 的48只 + 510300; 成交 `next_open`.

#### 参考来源 (吸收点, 非照搬)
- `WAYLON/ashare-quant-strategies`: 斜率×R² 动量、黄金/海外/成长/价值小池轮动、风险均衡多资产、RSRS 择时
- `zhangsensen/etf-rotation-strategy`: 执行>信号; top2 + 迟滞 + 波动率门控; 强调不做硬择时
- `Alpharotationlab/A-Share-ETF-Rotation-Model`: mom20 + 长均线过滤; 2019/2024 宽基年易跑输

#### V11 关键结论
1. **全天候验收改口径**: 不只看全样本收益, 看 `beat_years` + `min_excess` + 回撤
2. **C01 单票** 主题年极强, 但 2024 类结构/宽基年会小幅输300
3. **pure_eff 持2只** 是当前最优全天候候选: 三年均正超额, 回撤更浅, 总收益略低于 C01 可接受
4. **G15(top2)** 进攻更好, 最差年超额接近 0, 作影子对照
5. **park_bench / prefer_bench** 本样本常砍超额 (2024 prefer 更差), 不作为默认全天候解
6. **signed_eff / abs_mom / dual_ma** 在本轮核心样本未稳定提升全天候分
7. 生产 **C01 继续冻结**; AW 仅研究配置 + `scripts/compare_allweather.py`
8. 信号侧 `portfolio.decide` 仍单持仓; top_n=2 完整实盘接线列为后续

结果入口:
- `python3 scripts/compare_allweather.py --count 640`
- 配置: `config/c01_aw.json`, `config/c01_aw_g15.json`

### V12 全天候深化 (2026-07-21)

| 版本 | 核心改动 | 收益 | 回撤 | 夏普 | 分年超额 | 最差超额 | 结论 |
|------|---------|:----:|:----:|:----:|:--------:|:--------:|------|
| V11-AW | pure_eff top2 | +104% | -12% | 3.0 | 3/3 | +3.4% | 稳, 总收益低于C01 |
| **V12-v3** | **+bm0.3 + soft_trend** | **+127%** | **-11%** | **3.9** | **3/3** | **+7.0%** | **当前最优全天候** |
| V12-slope | slope_r2 top2 | +49% | -25% | 0.8 | 1/3 | -9% | 本样本失败 |
| V12-multi | +黄金/纳指 | +89% | -15% | 2.1 | 2/3 | -0.4% | 未超 pure_eff |

实现:
1. `factors.slope_r2` = 对数价回归 `ann_ret * R²` (参考社区多资产动量)
2. 回测 `soft_trend`: 趋势关 → 清行业、停靠300, 不强制空仓现金
3. 配置透传 `bm` / `extra_universe` / `soft_trend`
4. 新配置: `c01_aw_v3.json` (主), `c01_aw_v2/slope/mix/multi`

关键结论:
1. **宽度门控 + 软趋势** 比换因子更有效 (执行/仓位路径 > 新信号)
2. 与 `zhangsensen` 结论同向: 执行纪律与门控回报更高
3. 斜率×R² 在本行业大池上过拟合差, 不替换 pure_eff
4. C01 仍冻结; v3 作研究主线, 待双持仓信号接线 + 更长 WF 后再谈上线

命令: `python3 scripts/compare_allweather.py --count 640`

### V13 本地缓存 + 长历史代理 (2026-07-21)

#### 本地 K 线缓存
- `etf_rotation/data.py`: 磁盘缓存 `$HOME/.cache/etf-rotation/klines`
- 二次取数同参数从 ~分钟级 → **几十毫秒**
- `qfq` 失败回退 `none`; 支持 `force_refresh` / `ETF_KLINE_CACHE`

#### 长历史代理宇宙
- 配置: `config/pool_long_proxy.json` (龙头股+宽基, 研究用)
- 脚本: `scripts/run_long_proxy.py`
- 样本: 2013-05-22~2026-07-21, 24 只交集, `adjust=none`, T+1

| 策略 | 收益 | 回撤 | beat年 | 最差超额 | 备注 |
|------|-----:|-----:|:------:|---------:|------|
| 510300 BH | +94% | -46% | - | - | 基准 |
| C01 | +172% | -33% | 8/14 | -29% | 总收益赢, 2014/15/19/24/25 等年超额差 |
| AW_v3 | +73% | -31% | 9/14 | -30% | 更稳一点但 10 年总收益弱 |
| park | +322% | -35% | 9/14 | -29% | 代理上底仓吃到宽基慢牛 |

#### V13 结论 (对 10 年持有)
1. **ETF 短历史不够验证全天候**; 必须长代理压力测试
2. 短样本 AW 优势在 10 年+ 代理上 **显著衰减**, 不能承诺「结构牛每年赢 300」
3. 长期更合理框架: **底仓宽基 + 有限卫星轮动**, 控制行业拥挤与空仓拖累
4. 随机长寿股票可做稳健性检验, 但有存活偏差, 不能替代 ETF 执行层
5. 生产 C01 仍冻结; 缓存与长代理脚本入库研究

### V14 长周期全天候 (股票代理, 2026-07-21)

问题: ETF 历史短, 短样本 AW 无法支撑 10 年持有叙事.
方法: `pool_long_proxy` + 本地 K 线缓存, 2013-05~2026-07 压力测试.

| 策略 | 长代理收益 | beat年 | 结构年* | 短ETF核心 | 结论 |
|------|----------:|:------:|:-------:|----------:|------|
| C01 | +172% | 8/14 | 弱 | 2/3 | 生产基线 |
| AW_v3 | +73% | 8/14 | 弱 | **3/3** | 短样本优 |
| park | +322% | 9/14 | 中 | 2/3 | 吃慢牛 |
| **AW_v4** | **+314%** | **11/14** | **更强** | 2/3 | **长周期主候选** |

\*结构年观察集: 2014/2017/2019/2024/2025

v4 规则:
- 权重同 C01 (`eff0.6+mtf0.4`)
- top1 卫星
- `prefer_bench_if_stronger=true` (行业 20 日弱于 300 则持 300)
- `soft_trend=true` (趋势关停靠 300, 不裸奔现金)
- `bm=0.3` 宽度门控
- `park_bench=true`

配置: `config/c01_aw_v4.json`  
命令: `python3 scripts/run_long_proxy.py --count 3200 --adjust none`

边界:
1. 股票代理有存活偏差, 不能直接当 ETF 期望收益
2. v4 在短样本 ETF 上 2024 仍可能小输 300, 但长样本赢年显著更多
3. 生产不切换; 先影子观察


### V15 扩池长周期确认 (2026-07-21)

动作:
1. 扩代理池 `pool_long_proxy_v2` (一赛道一只, 27 只, 2013-07 起)
2. v4 邻域再搜: bm / stop / top2 / pure / g15
3. 旧代理 + 新代理 + 短ETF 三方迁移

| 策略 | 旧代理 beat | 新代理 beat | 短ETF核心 | 结论 |
|------|------------:|------------:|:---------:|------|
| C01 | 8/14 | 3/11 | 2/3 | 生产 |
| v3 | 8/14 | 4/11 | 3/3 | 短样本 |
| **v4** | **11/14** | **6/11** | 2/3 | **长周期主线** |
| v4.1 stop-10% | 11/14 | 6/11 | 2/3 | 扩池收益更高, 不替换 v4 |

结论:
1. 扩池后结构年仍常输 300, 说明「全天候=每年赢」不现实
2. v4 在两套长代理上都明显优于纯 C01/v3 的长周期赢年
3. stop 加严 (v4.1) 可作影子, 暂不升级主候选
4. 继续保持: 生产 C01 冻结; 长测走本地缓存

命令:
```bash
python3 scripts/run_long_proxy.py --pool pool_long_proxy --count 3200 --adjust none
python3 scripts/run_long_proxy.py --pool pool_long_proxy_v2 --count 3200 --adjust none
```

### V16 风险清单验收 (控回撤优先, 2026-07-21)

你给的标准:
- 夏普 ≥ 1.0 (A股可放宽 0.8/1.2)
- 最大回撤 < 15% (至少 < 40%)
- 卡玛 > 1
- 期望值 > 0
- 样本外不背离 / 前向效率 ≥ 50%
- 牛熊震荡分段
- 含成本

引擎补强:
- `backtest` 新增真实 `sharpe` / `calmar` / `expectancy` / `payoff`
- 旧 `sp` 保留为年化/回撤近似, 兼容老脚本

长代理 (2013-05~2026-07, 含成本) 基线对照:

| 策略 | 年化 | 回撤 | 夏普 | 卡玛 | 期望 | 清单观感 |
|------|-----:|-----:|-----:|-----:|-----:|----------|
| C01 | +10.0% | -33% | 0.52 | 0.30 | +0.74% | 回撤/夏普不达标 |
| v3 | +5.4% | -31% | 0.34 | 0.17 | +0.30% | 更不达标 |
| v4 | +14.6% | -33% | 0.67 | 0.44 | +0.93% | 收益强, 回撤仍差 |
| **DEF** | +11.6% | **-23%** | **0.69** | **0.50** | +0.82% | **回撤明显改善** |

短ETF核心上 DEF 回撤 -13.3%, 仍具备可用性.

配置:
- 防守版: `config/c01_aw_def.json`  (ps=0.70, stop=-5%, v4门控)
- 完整网格后台: `scripts/audit_risk.py` (分批) + `output/risk_audit/`
- 低回撤特攻后台: `output/risk_audit/lowdd_runner.log`

结论:
1. 你说得对: **当前最大问题是回撤**, 不是不会赚
2. 在长代理上, 现有进攻型 AW **达不到** 回撤<15%/夏普≥1 的严标准
3. 有效降回撤路径已验证: **降仓(≤70%) + 底仓/相对强弱 + 紧止损**
4. 下一步不是继续抬收益, 而是把回撤压到 25% 内再谈超额

命令:
```bash
# 看后台完整网格进度
tail -f output/risk_audit/runner.log
# 合并 (全部 batch 完成后)
python3 scripts/audit_risk.py --merge --max-dd 0.40 --sharpe 0.8
# 防守版回测
python3 scripts/run_backtest.py --strategy c01_aw_def --count 640
python3 scripts/run_long_proxy.py --strategies c01,c01_aw_v4,c01_aw_def --count 3200 --adjust none
```


### V17 控回撤结论 (清单验收, 2026-07-21)

核心事实: 现有进攻型轮动在长代理上 **回撤不合格**.

长代理 2013-05~2026-07 (含成本) 对照:

| 策略 | 仓位 | 年化 | 回撤 | 夏普 | 卡玛 | 清单通过 |
|------|-----:|-----:|-----:|-----:|-----:|---------:|
| C01 | 95% | +10.0% | -33.2% | 0.52 | 0.30 | 5/11 |
| AW_v4 | 95% | +14.6% | -33.0% | 0.67 | 0.44 | 3/11 |
| DEF70 | 70% | +11.6% | -23.1% | 0.69 | 0.50 | 4/11 |
| **DEF40** | **40%** | +6.7% | **-13.3%** | 0.68 | 0.50 | **8/11** |
| **DEF30** | **30%** | +5.4% | **-9.7%** | 0.71 | 0.55 | **8/11** |

按你清单:

| 检验维度 | 合格标准 | DEF30 | 状态 |
|---------|----------|------:|------|
| 夏普 | ≥1.0 (可放宽0.8) | 0.71 | 未过 |
| 最大回撤 | <15% (至少<40%) | **-9.7%** | **过15%线** |
| 卡玛 | >1 | 0.55 | 未过 |
| 期望值 | >0 | +0.91%/笔 | 过 |
| 样本外 | 不显著背离 | 前向效率84% | 过 |
| 多状态 | 熊市可活 | 熊市回撤约-4.9% | 过 |
| 含成本 | 扣费后仍盈 | 是 | 过 |

结论:
1. 你说得对: 之前方案主要问题是回撤, 不是盈利
2. **把仓位降到 30%~40% + v4 防守门控**, 是当前唯一稳定压回撤路径
3. DEF30 已过“回撤<15%”, 但仍未过“夏普≥0.8/1.0 且卡玛>1”
4. 若目标是长期持有舒适度: **优先 DEF30/DEF40**; 若目标是进攻超额: 才看 v4, 并接受 -30%+ 回撤
5. 生产 C01 继续冻结; 风控版仅研究/影子

配置:
- `config/c01_aw_def30.json`  主推荐风控版
- `config/c01_aw_def40.json`  折中版
- `config/c01_aw_def.json`    70仓过渡版

命令:
```bash
python3 scripts/run_long_proxy.py --strategies c01,c01_aw_v4,c01_aw_def,c01_aw_def40,c01_aw_def30 --count 3200 --adjust none
python3 scripts/run_backtest.py --strategy c01_aw_def30 --count 640
```



### V18 同仓位真质量搜索 (2026-07-21)

约束: **固定 `ps=0.95`**, 不用降仓伪装回撤. 质量看夏普/卡玛/样本外, 并做 ETF 迁移.

#### 长代理 (2013-05~2026-07, 含成本)

| 策略 | 年化 | 回撤 | 夏普 | 卡玛 | OOS夏普 | FAE | beat | 备注 |
|------|-----:|-----:|-----:|-----:|-------:|----:|-----:|------|
| C01 | +10.0% | -33.2% | 0.52 | 0.30 | 0.44 | 96% | 8/14 | 基线 |
| AW_v4 | +14.6% | -33.0% | 0.67 | 0.44 | 0.64 | 41% | 11/14 | 进攻全天候 |
| **Q8** `rb8+oh40+inv` | +19.9% | **-23.3%** | **0.86** | **0.85** | **0.91** | 109% | 8/14 | 代理最优, ETF塌 |
| **Q10** `rb10+oh40+inv` | +23.1% | -27.8% | **0.97** | **0.83** | 0.69 | 62% | 8/14 | **迁移更稳** |

相对 C01 (Q8): 夏普 +0.34, 卡玛 +0.55, 回撤改善约 10pct, 年化 +9.8pct.  
这是 **规则质量变化**, 不是仓位缩放.

#### 迁移检验 (关键)
| 策略 | 长代理 | ETF核心 | 结论 |
|------|--------|---------|------|
| Q8 | 最优 | 年化+9.8% / 回撤-39% / 夏普0.46 | **过拟合代理, 不上线** |
| **Q10** | 很强 | 年化+41.8% / 回撤-16.7% / 夏普1.41 | **可作研究影子** |
| C01 | 基线 | 年化+41.7% / 回撤-14.8% / 夏普1.47 | 生产冻结 |
| v4 | 稳 beat | 年化+53.1% / 回撤-17.3% / 夏普1.77 | 短ETF仍强 |

#### 有效杠杆 (同仓位)
1. **`rb_days` 10 附近** — 加快轮动, 抬夏普 (8 在代理更强但 ETF 不稳)
2. **`overheat=0.40`** — 略放宽过热过滤, 减少踏空
3. **`inv_vol=true`** — 波动升高自动降仓, **同名义ps下真压回撤**
4. 单纯 dual_ma / slope_r2 / 再降 stop / top2: 本轮未稳定同时抬夏普+迁移

#### 结论
1. 存在 **同仓位真改进**, 不是降仓假象
2. 研究主候选: **`c01_q10`** (迁移优先)
3. 代理对照: `c01_q8` (只研究, 不影子)
4. 仍未同时满足 `夏普≥1 + 卡玛>1 + 回撤<15%` 的长代理全样本严标准
5. 生产 C01 继续冻结

配置:
- `config/c01_q10.json`
- `config/c01_q8.json`

命令:
```bash
python3 scripts/run_long_proxy.py --strategies c01,c01_aw_v4,c01_q10,c01_q8 --count 3200 --adjust none
python3 scripts/run_backtest.py --strategy c01_q10 --count 640
```


### V19 Q10 邻域深挖 (同仓位, 2026-07-21)

目标: 在 **固定 ps=0.95** 下继续找真 alpha, 并强制 ETF 迁移验收.

#### 结果
**Q10 已接近局部最优.** 本轮没有迁移稳健的实质性超越.

| 候选 | 长代理夏普 | 长代理回撤 | ETF夏普 | ETF回撤 | 判定 |
|------|----------:|----------:|--------:|--------:|------|
| **Q10** | **0.97** | -27.8% | **1.41** | -16.7% | **主候选不变** |
| Q10+hyst0.15 | 0.97 | -27.8% | 1.42 | -16.7% | 噪声级微调 |
| Q10+oh0.35 | 0.97 | -27.7% | 1.41 | -16.7% | 噪声级微调 |
| Q10+v4light | 0.82 | -26.6% | 1.68 | -16.8% | ETF好但长代理质量退 |
| Q10+prefer | 0.82 | -29.1% | 0.70 | -35.4% | 失败 |
| Q10+trail8 | 0.92 | -27.7% | 0.65 | -35.3% | ETF 塌 |
| Q10+top2 | 0.55 | -27.3% | 1.61 | -17.4% | 伤夏普 |
| Q8 | 0.86 | -23.3% | 0.46 | -39.2% | 代理过拟合 |
| C01 | 0.52 | -33.2% | 1.47 | -14.8% | 生产冻结 |
| v4 | 0.67 | -33.0% | 1.77 | -17.3% | beat年强, 夏普弱于Q10 |

#### 本轮无效方向
1. 改因子权重 (pure/g15/em20/m60/signed/abs)
2. 在 Q10 上硬叠 prefer / 重 v4 门控
3. trail 止损 (ETF 不稳)
4. top2 分散 (夏普明显掉)
5. 关掉 inv_vol (回撤回到 -32%)

#### 仍有效的三件套
`rb=10 + overheat=0.40 + inv_vol=true` (C01 权重)

#### 结论
1. 同仓位真改进 **已经找到并收敛到 Q10**
2. 继续在同一骨架上拧参数, 收益是噪声
3. 下一跳应换 **结构**, 而不是再扫 rb/oh:
   - 新因子定义 (非现有 zscore 组合)
   - 动态空仓/半仓状态机 (非线性降仓)
   - 可交易多资产底仓 (债/黄金/海外) 的真实对冲
   - 行业池去相关与执行层 ETF 映射
4. 生产 C01 继续冻结; Q10 仅研究影子


### V20 结构型探索 (新因子/状态仓/对冲, 2026-07-21)

同骨架拧参已收敛后, 改结构:
1. 新因子: `mom_vol` / `accel` / `up_ratio` / `rel_m20`
2. 非线性状态仓 `state_pos` (趋势强度+波动缩放)
3. 多资产停靠 `park_assets` (黄金/纳指/防御)
4. **修复 soft_trend 底仓买入**: 旧逻辑趋势关时只清仓不买停靠 (变相现金), 与文档意图不符

#### 引擎改动
- `factors.py`: 结构因子
- `backtest.py`: `state_pos` / `park_scale` / `park_assets`; soft 停靠可买入
- `config.py`: 透传上述参数

#### 固定 ps=0.95 结果 (长代理 + ETF)

| 候选 | 长代理夏普 | 回撤 | ETF夏普 | ETF回撤 | 判定 |
|------|----------:|-----:|--------:|--------:|------|
| **Q10** | **0.97** | -27.8% | **2.19** | -13.4% | **仍主候选** |
| state_pos | 0.82 | -24.5% | 2.13 | -12.9% | 回撤略好, 质量下降 |
| softfix (Q10+soft) | 1.01 | -38.0% | 1.27 | -40.9% | 收益高但回撤差 |
| gold+nq 停靠 | 1.01 | -38.0% | 2.35 | -23.8% | 未稳超 Q10 |
| 新因子 mom_vol/up/rel | ≤0.75 | 更差 | 一般 | — | **失败** |
| extra 黄金纳指打分 | ≈Q10 | ≈Q10 | ≈Q10 | — | 无增量 |
| top2+extra | 0.55 | -27% | 高 | — | 伤夏普 |

#### soft_trend 数字修正
修复后 `c01_aw_v4` 长代理变为约 夏普0.54 / 回撤-51% (旧数字偏乐观, 因趋势关时常掉进现金).  
**Q10 不受影响** (soft=false). 历史 AW soft 系列结论需按修复后重读.

#### 结论
1. 结构三件套本轮 **没有迁移稳健地打赢 Q10**
2. 新因子定义失败; 状态仓是另一种风险预算, 不是更高 alpha
3. 对冲停靠在本池/本规则下未成为稳定优势
4. 研究主线仍是 **Q10**; 生产 C01 继续冻结
5. 下一跳应更“外面”: 可交易债金独立状态机, 或行业中性/配对, 或完全不同的信号源


### V21 双袖 Regime / 状态现金 (2026-07-21)

目标: 进攻袖 (Q10) + 防御袖/现金 的 **状态依赖分配**, 不是固定降仓.

#### 研究发现
1. **防御资产袖失败**: 电力/石油/黄金股动量停靠, 长代理防御袖本身夏普≈0~0.15, 拖累组合
2. **现金 regime 有效** (研究混合器, 日频再平衡):
   - `flip`: bull/chop 进攻, riskoff/bear 现金 → 长代理夏普约 1.5, 回撤约 -19%
   - 明显优于裸 Q10 (0.97 / -28%)
3. 这与 `ps=0.5` 线性缩仓不同: 线性缩仓夏普几乎不变 (0.95), 只等比压回撤; regime 是 **状态开关**

#### 引擎落地
- `regime_map`: `{bull,chop,riskoff,bear} -> 进攻仓位比例`
- 非调仓日也会按 regime 下调暴露 (减仓到现金)
- 配置:
  - `config/c01_q10_rg.json`  dual_C 风格
  - `config/c01_q10_rg_flip.json` 硬切换

#### 引擎实盘路径验收 (next_open, 含成本)

| 策略 | 长代理夏普 | 回撤 | 年化 | ETF夏普 | ETF回撤 |
|------|----------:|-----:|-----:|--------:|--------:|
| Q10 | 0.97 | -27.8% | +23.1% | 2.19 | -13.4% |
| **Q10_rg_flip** | **1.04** | -27.0% | +22.9% | 2.02 | -13.4% |
| Q10_rg | 0.91 | -28.9% | +17.0% | 1.67 | -13.4% |
| Q10 ps=0.5 线性 | 0.95 | -15.2% | +12.0% | 2.12 | -8.0% |

说明: 引擎版 flip **小幅**抬升长代理夏普 (0.97→1.04), 但弱于研究混合器 (因 next_open 减仓粒度粗, 非完美日频再平衡). ETF 端未超越裸 Q10.

#### 结论
1. 真正有信号的是 **risk-off 切现金**, 不是切到烂防御资产
2. `c01_q10_rg_flip` 可作为 Q10 的状态风控影子
3. 仍未同时满足长代理 `夏普≥1 + 卡玛>1 + 回撤<15%`
4. 生产 C01 继续冻结


### V22 组合波动目标 (2026-07-21)

目标: 在 Q10 信号不变下, 用 **已实现组合波动** 动态缩放总暴露.

#### 为什么不是假降仓
| 方法 | 长代理夏普 | 回撤 | 卡玛 | 说明 |
|------|----------:|-----:|-----:|------|
| Q10 ps=0.95 | 0.97 | -27.8% | 0.83 | 基线 |
| Q10 ps=0.70 线性 | 0.95 | -20.9% | 0.81 | 夏普几乎不变 |
| Q10 ps=0.50 线性 | 0.95 | -15.2% | 0.79 | 假降仓 |
| **Q10_vt15** | **1.14** | **-23.0%** | **1.04** | **真改进** |
| **Q10_vt12** | **1.15** | **-22.6%** | 0.98 | 更稳 |

线性缩仓: 夏普卡住 ~0.95.  
波动目标: 夏普抬到 1.14+, **卡玛首次过 1**.

#### 引擎验收 (next_open, 含成本)

| 策略 | 长代理夏普 | 回撤 | 年化 | OOS夏普 | ETF夏普 | ETF回撤 |
|------|----------:|-----:|-----:|--------:|--------:|--------:|
| Q10 | 0.97 | -27.8% | +23.1% | 0.69 | 2.19 | -13.4% |
| rg_flip | 1.04 | -27.0% | +22.9% | 0.80 | 2.02 | -13.4% |
| **vt12** | **1.15** | **-22.6%** | +22.1% | **1.26** | 2.15 | **-7.3%** |
| **vt15** | **1.14** | **-23.0%** | **+24.0%** | 1.07 | **2.40** | **-7.5%** |

#### 实现
- `vol_target` / `vol_lookback` / `vol_wmin` / `vol_wmax`
- 用组合日收益滚动标准差年化, `w = target / realized`, 夹紧后乘到 `trg`
- 超配时主动减仓到现金

配置:
- `config/c01_q10_vt15.json`  **当前质量主候选**
- `config/c01_q10_vt12.json`  更保守

#### 结论
1. **波动目标是本轮最强真 alpha/风险预算改进**
2. 优于 regime flip 与固定降仓
3. 长代理仍未同时满足 `夏普≥1 + 卡玛>1 + 回撤<15%` (卡玛刚过1, 回撤仍~23%)
4. 生产 C01 继续冻结; vt15/vt12 仅研究影子


### V23 波动目标精调 (2026-07-21)

在 vt15 邻域扫 target/lookback/floor, 并试 VT+regime.

#### 引擎验收 (固定信号 Q10, 只改暴露)

| 策略 | 目标波动 | 长代理夏普 | 卡玛 | 回撤 | OOS夏普 | ETF夏普 | ETF回撤 |
|------|--------:|----------:|-----:|-----:|--------:|--------:|--------:|
| Q10 | — | 0.97 | 0.83 | -27.8% | 0.69 | 2.19 | -13.4% |
| vt15 | 15% | 1.14 | 1.04 | -23.0% | 1.07 | 2.40 | -7.5% |
| vt12 | 12% | 1.15 | 0.98 | -22.6% | 1.26 | 2.15 | -7.3% |
| **vt13** | **13%** | **1.27** | **1.17** | -22.8% | 1.12 | 2.14 | -7.6% |
| **vt11** | **11%** | **1.22** | 1.06 | -22.2% | **1.22** | **2.39** | **-6.9%** |

#### 发现
1. **最优目标波动在 11%~13%**, 不是 15%
2. `vol_lookback=20` 稳定; 15/30 通常更差
3. `vol_wmin` 0.10~0.20 差异很小 (常等价)
4. VT + regime soft/flip **未稳超** 纯 VT
5. 关 `inv_vol` 在低 target 时接近, 高 target 时变差 → 保留 inv_vol

#### 结论
- 质量主候选切换为 **`c01_q10_vt13`**
- 迁移/OOS 更均衡备选: **`c01_q10_vt11`**
- 仍未同时满足 `回撤<15%` (最好约 -22%)
- 生产 C01 继续冻结

配置:
- `config/c01_q10_vt13.json`
- `config/c01_q10_vt11.json`


### V24 防过拟合硬验收 (2026-07-21)

按用户标准重验, **不再以全样本夏普排名选主候选**.

脚本: `scripts/validate_robust.py`

#### 验收标准
1. 样本外: FAE≥50%, OOS 盈利, 不塌缩, 回撤≤40%
2. 前向分析 (IS=3y, OOS=1y, 滚动):
   - 总体盈利
   - 平均 WFE ≥50%
   - ≥50% 窗口盈利
   - 无单窗利润 >50%
   - 回撤 ≤40%
3. 多状态: 非单一年份/单一状态吃利润, 熊市可活, ≥2 状态正均收益

#### 长代理结果 (2013-05~2026-07)

| 策略 | 全样本夏普 | 回撤 | 状态 | 硬伤 |
|------|----------:|-----:|------|------|
| C01 | 0.52 | -33.2% | **不合格** | FAE=48%<50% |
| Q10 | 0.97 | -27.8% | 观察 13/14 | 牛市利润占比 78% |
| **VT11** | 1.22 | -22.2% | **合格 14/14** | 无 |
| VT13 | **1.27** | -22.8% | 观察 13/14 | 牛市利润占比 75% |
| VT15 | 1.14 | -23.0% | 观察 13/14 | 牛市利润占比 72% |
| RG_flip | 1.04 | -27.0% | 观察 13/14 | 牛市利润占比 80% |
| AW_v4 | 0.54 | -51.2% | **不合格** | OOS 塌缩 + 回撤>40% + FAE=18% |

#### VT11 前向细节 (关键)
- 11 窗, 总前向收益 +257%, 平均 WFE **83%**, 盈利窗 **82%**
- 最大单窗利润份额 20% (<50%)
- 熊市 3 年均值收益 +15.9%, 最差熊市回撤 -6.2%
- 牛/熊/震利润不过度集中 (max state 65%)

#### VT13 为何降级
全样本更漂亮 (夏普 1.27 / 卡玛 1.17), 但:
- 多状态: 牛市贡献约 75% 正利润 → 普适性弱于 VT11
- 前向仍过, 却不是最稳

#### 结论
1. **防过拟合主候选: `c01_q10_vt11`**
2. VT13/VT15 仅作进攻对照, 不因全样本更美而上位
3. C01 / AW_v4 在本标准下不合格
4. 生产 C01 继续冻结; 影子优先 VT11
5. 漂亮回测 ≠ 可上实盘; 以后新候选必须先过 `validate_robust.py`

命令:
```bash
python3 scripts/validate_robust.py \
  --strategies c01,c01_q10,c01_q10_vt11,c01_q10_vt13,c01_q10_vt15 \
  --pool pool_long_proxy --count 3200
```


### V25 多 Agent 并行搜索骨架 (2026-07-21)

目标: 多研究轴并行提速, 受 CPU 护栏约束, 晋级必须过 `validate_robust`.

#### 组件
- `etf_rotation/search_spec.py` — 候选协议 + 四轴生成 (vt/regime/factor/stop_rb)
- `etf_rotation/search_data.py` — 共享池加载 / materialize_params
- `scripts/search_lanes.py` — init / run-lane / merge / gate / promote
- `scripts/search_dispatch.py` — 一键编排 + `max_agents*workers<=4` 护栏
- `scripts/validate_robust.py` — `--gate` / `--require-pass` / `--from-shortlist` / `--preset`

#### 并发模型
- 切片数可多 (10–20 队列)
- 同时重回测: 默认 2–4 路; 硬上限 worker 积 ≤4
- 写集互斥: `output/search_runs/<run_id>/lanes/<lane>/**`
- merge/gate 单线程

#### 命令
```bash
# 一键 (推荐本机)
python3 scripts/search_dispatch.py --run-id auto --axes vt,regime,factor,stop_rb --max-agents 2 --workers 2

# 或手工
python3 scripts/search_lanes.py init --run-id R1 --axes vt,stop_rb
python3 scripts/search_lanes.py run-lane --run-id R1 --lane vt --workers 2
python3 scripts/search_lanes.py run-lane --run-id R1 --lane stop_rb --workers 2
python3 scripts/search_lanes.py merge --run-id R1 --top 30
python3 scripts/search_lanes.py gate --run-id R1
# 仅当 long 合格且 etf 非不合格:
python3 scripts/search_lanes.py promote --run-id R1 --candidate <id>

# 门禁
python3 scripts/validate_robust.py --preset long_proxy --gate --require-pass c01_q10_vt11
```

#### 规则
1. 生产 `c01` 冻结; promote 只写 `research:true`
2. 线性降仓 `ps<0.9` 默认剔出 shortlist
3. 晋级唯一标准: `validate_robust` 合格, 不是全样本夏普最高


### V26 合格线索固化 + ETF 宽松档 + focus 再搜 (2026-07-21)

#### 固化配置 (研究影子, 生产仍冻结 c01)
| 配置 | 结构 | 长代理 | ETF 宽松 | 用途 |
|------|------|--------|----------|------|
| `c01_q10_vt11` | vt=0.11 | 14/14 | 合格 | 防过拟合主候选 |
| `c01_q10_vt09` | vt=0.09 | 14/14 | 合格 | 双端候选 |
| **`c01_q10_vt09_oh35`** | **vt=0.09 oh=0.35** | **14/14 sh1.33 dd-17.6%** | **合格** | **本轮 focus 最优** |
| `c01_q10_vt11_oh35` | vt=0.11 oh=0.35 | 14/14 dd-18.0% | 合格 | 回撤优于标准 vt11 |
| `c01_q10_vt11_rb8` | rb=8 | 14/14 | **不合格** | 仅长代理对照 |
| `c01_q10_vt11_rb8_st5` | rb=8 st=-5% | 14/14 dd-14.8% | **不合格** | 仅长代理对照 |

#### ETF 宽松验收档 (`--preset etf_soft`)
- 不套 10 年 WF / 多状态 (短样本构造成灭)
- 5 项: FULL 有数据/盈利/dd≤40%/sh≥0.8 + OOS 可接受 (ret>-15% 且非 (ret<0 & sh<0.3))
- 晋级规则更新: **长代理 14/14 合格 + etf_soft 非不合格**; etf_core 仅对照

#### focus 再搜 (`run_focus_vt09`, 56 候选)
- 轴: vt∈{0.08..0.12}×lb×wmin + mild rb{9,10,11}×stop×oh
- 全样本最优: `focus_vt9_rb10_st8_oh35` (sh1.33 cal1.34 dd-17.6%)
- 长代理 top7 全 14/14; etf_soft 仅 `rb9_st6_oh35` 不合格
- 关键结构线索: **overheat 0.40→0.35** 在 vt09/vt11 上同时降回撤且双端过门

#### 命令
```bash
python3 scripts/validate_robust.py --preset etf_soft
python3 scripts/validate_robust.py --preset long_proxy --strategies c01_q10_vt09_oh35,c01_q10_vt11,c01_q10_vt09
# gate 现含 etf_soft
python3 scripts/search_lanes.py gate --run-id <R>
```

#### 结论
1. 防过拟合主候选仍可保留 **vt11**; 本轮质量/回撤更优的双端候选是 **vt09_oh35**
2. rb=8 族长代理漂亮但 ETF 后半段失效 → 不晋级
3. 生产 c01 继续冻结; 影子可并行 vt11 / vt09_oh35

### V27 扩展轴 oh_vt/exec + ProcessPool 加固 (2026-07-21)

#### 实现
- 新轴 `oh_vt`: overheat×vol_target 正交 (含 rb 微调), ≤48
- 新轴 `exec`: min_hold/hyst/stop 在 `vt09_oh35` 与 `vt11` 基底
- `stop_rb` 补 `overheat=0.35`
- 锚点加 `anchor_c01_q10_vt09_oh35`
- ProcessPool: worker initializer 预载数据 + 单任务 timeout + 完成率不足整 lane 串行回退

#### 运行 `20260721_182746` (oh_vt + exec, workers=1×2)
- 74 metrics; shortlist 后结构门禁 10 条
- 长代理: vt09/10/11+oh35 **14/14**; vt13 族 **观察 13/14**
- ETF soft: 门禁名单 **全部合格**
- exec: mh/hyst 对最优几乎无增量 (同构 metrics)

#### 固化
| 配置 | 结构 | 长代理 | ETF soft |
|------|------|--------|----------|
| `c01_q10_vt09_oh35` | 已有 | 14/14 sh1.33 dd-17.6% | 合格 |
| **`c01_q10_vt10_oh35`** | **vt=0.10 oh=0.35** | **14/14 sh1.18 dd-17.8%** | **合格** |
| `c01_q10_vt11_oh35` | 已有 | 14/14 | 合格 |

#### 结论
1. **oh=0.35 是可复现结构** (非单点运气), 在 vt 0.09–0.11 带双端过门
2. 研究主线并列: `vt11` (原防过拟合锚) + `vt09_oh35` (质量/回撤最优)
3. exec 微调优先级低; 下一轮应扩正交结构 (regime×oh_vt / factor×vt) 而非再扫 mh
4. 生产 c01 继续冻结

```bash
python3 scripts/search_dispatch.py --run-id auto --axes oh_vt,exec --max-agents 2 --workers 1
python3 scripts/validate_robust.py --preset long_proxy --strategies c01_q10_vt09_oh35,c01_q10_vt10_oh35,c01_q10_vt11
python3 scripts/validate_robust.py --preset etf_soft --strategies c01_q10_vt09_oh35,c01_q10_vt10_oh35
```



### V28 regime×oh_vt + factor×vt 正交 (2026-07-21)

#### 实现
- 新轴 `regime_oh`: 4 regime_map × (vt,oh) 双端带 + none 对照
- 新轴 `factor_vt`: 8 因子权重 × (vt09oh35 / vt11) + 部分 top2

#### 运行 `20260721_184421`
- 43 metrics; 结构门禁 10 条
- 长代理:
  - `soft+vt09+oh40` **14/14** sh1.30 **dd-13.9%** cal1.55
  - `flip+vt09+oh35` **14/14** sh1.23 dd-13.8% cal1.40
  - `soft+vt09+oh35` 14/14 (略逊)
  - dualB 观察; pure 因子 **不合格**
- ETF soft: 门禁名单全部合格

#### 固化
| 配置 | 结构 | 长代理 | 用途 |
|------|------|--------|------|
| **`c01_q10_vt09_soft_oh40`** | soft regime + vt9 + oh40 | **sh1.30 dd-13.9%** | **本轮回撤最优双端** |
| `c01_q10_vt09_flip_oh35` | flip + vt9 + oh35 | sh1.23 dd-13.8% | 激进 riskoff 空仓对照 |
| `c01_q10_vt09_oh35` | 无 regime | sh1.33 dd-17.6% | 质量最优无状态机 |
| `c01_q10_vt11` | 无 regime | sh1.22 dd-22.2% | 防过拟合原锚 |

#### 结论
1. **状态仓位 × 波动目标** 比换因子更能压回撤, 且不过拟合门禁
2. 因子权重保持 **c01w (eff/mtf)**; pure/g15 不晋级
3. 研究影子三线并行: `vt09_soft_oh40` (低回撤) / `vt09_oh35` (高夏普) / `vt11` (原锚)
4. 生产 c01 继续冻结

```bash
python3 scripts/search_dispatch.py --run-id auto --axes regime_oh,factor_vt --max-agents 2 --workers 1
python3 scripts/validate_robust.py --preset long_proxy --strategies c01_q10_vt09_soft_oh40,c01_q10_vt09_oh35,c01_q10_vt11
```


### V29 soft 状态细网格 (2026-07-21)

#### 实现
- 新轴 `soft_grid`: soft 的 chop×riskoff 细网格 (主 vt09+oh40, 辅 oh35/vt11)
- 新轴 `soft_stack`: 固定 soft map 扫 vt×oh
- 锚点加 `c01_q10_vt09_soft_oh40`

#### 运行 `20260721_190113` (80 candidates)
- 长代理门禁 12 条 **全部 14/14** (含对照)
- ETF soft **全部合格**
- 新最优: **`soft + vt=0.08 + oh=0.38`** sh**1.36** dd**-13.3%** cal**1.62**

#### 固化
| 配置 | 结构 | 长代理 | 用途 |
|------|------|--------|------|
| **`c01_q10_vt08_soft_oh38`** | soft+vt8+oh38 | **sh1.36 dd-13.3%** | **低回撤双端新主** |
| `c01_q10_vt08_soft_oh40` | soft+vt8+oh40 | sh1.35 dd-13.3% | 近同构对照 |
| `c01_q10_vt09_soft_oh40` | soft+vt9+oh40 | sh1.30 dd-13.9% | V28 候选 |
| `c01_q10_vt09_oh35` | 无 regime | sh1.33 dd-17.6% | 高夏普无状态机 |
| `c01_q10_vt11` | 无 regime | sh1.22 dd-22.2% | 防过拟合原锚 |

#### 结论
1. soft 结构上 **再降 vol_target 到 0.08** 仍双端合格, 夏普/回撤双升
2. soft_grid 显示 **chop≈0.90 稳健**; riskoff 在本样本近惰性 (c90 全 riskoff 同指标)
3. 勿再无脑扩 riskoff 网格; 下一正交优先: soft×rb/stop 或 shadow 信号接线
4. 生产 c01 继续冻结

```bash
python3 scripts/search_dispatch.py --run-id auto --axes soft_grid,soft_stack --max-agents 2 --workers 1
python3 scripts/validate_robust.py --preset long_proxy --strategies c01_q10_vt08_soft_oh38,c01_q10_vt09_soft_oh40,c01_q10_vt11
```


### V30 soft×rb/stop 正交 (平台确认) (2026-07-21)

#### 实现
- 新轴 `soft_rb`: soft+vt08 上 rb×stop×oh + vt09 辅扫
- 新轴 `soft_vt_rb`: soft 固定 oh38 扫 vt×rb
- 锚点加 `c01_q10_vt08_soft_oh38`

#### 运行 `20260721_192449` (82 candidates)
- 长代理: 基线与多数邻域 **14/14**
- ETF soft: **rb=8 族不合格**; rb=10 基线合格
- **无新结构超越** `vt08_soft_oh38` (sh1.36 dd-13.3%)

#### 结论 (停止乱扫)
1. soft 主线 **rb 锁定 10**; rb8 长代理好看但 ETF 后半段失效
2. stop/oh 微调近同构, 边际为零
3. vt 再降到 0.07 无效
4. **研究主线冻结为**:
   - 低回撤: `c01_q10_vt08_soft_oh38`
   - 高夏普无状态机: `c01_q10_vt09_oh35`
   - 防过拟合原锚: `c01_q10_vt11`
5. 下一优先 **非参数网格**: shadow 信号接线 / 实盘 decide 多持仓·vol_target 透传 / 成本敏感性
6. 生产 c01 继续冻结; **no_new_pass**

```bash
python3 scripts/search_dispatch.py --run-id auto --axes soft_rb,soft_vt_rb --max-agents 2 --workers 1
# 期望: 不产生新 promote
```


### V31 信号暴露接线 + 成本敏感性 (2026-07-21)

参数网格已平台 (V30). 本轮切 **非网格**: 信号侧风险预算接线 + 成本扫描.

#### 1. 信号暴露 (`portfolio.compute_target_exposure`)
- `position_pct × inv_vol × regime_map × vol_target(基准波动代理)`
- `decide(..., bench_bars=)` 返回 `target_exposure` / `exposure`
- `execute` 买入仓位用 `target_exposure` (不再死写 `position_pct`)
- 趋势关 → 暴露 0 (非 soft_trend)
- **注**: 信号 vol_target 用基准代理, 回测用组合已实现; 影子研究可接受, 非逐笔复现

#### 2. 研究影子
- `run_signal --shadow c01_q10_vt08_soft_oh38` (默认已切到研究主线; 旧 `c13_shadow` 仍可用)
- research 配置走完整 `decide` (不交易), 报告输出暴露/regime/vol_scale
- `--bars` 默认 120 (regime 需 MA60)
- 生产默认 `--strategy c01` **未改**

#### 3. 成本敏感性 `scripts/cost_sensitivity.py`
长代理 commission ∈ {0, 3bp, 10bp, 20bp}, 格式 sharpe/dd:

| 策略 | 3bp | 20bp | 衰减(20/3) |
|------|-----|------|------------|
| c01 | 0.52/-33% | 0.30/-36% | **0.58 ⚠** |
| q10 | 0.97 | 0.74 | 0.76 |
| vt11 | 1.22 | 0.90 | 0.74 |
| vt09_oh35 | 1.33 | 0.93 | 0.70 |
| **vt08_soft_oh38** | **1.36/-13.3%** | **0.99/-14.6%** | **0.73** |
| vt09_soft_oh40 | 1.30 | 1.05 | **0.81 最稳** |

结论: 研究主线在 20bp 仍 sh≈1; soft 族成本韧性优于裸 c01. 非零成本假优质.

#### 命令
```bash
python3 scripts/run_signal.py --dry-run --shadow c01_q10_vt08_soft_oh38
python3 scripts/cost_sensitivity.py
```

#### 边界
- 生产 c01 冻结; 默认策略未切换
- 未改 cron / GitHub Action 默认参数 (仍 c01; shadow 默认名若部署需显式确认)
- 下一优先: 影子独立状态文件 / 组合已实现 vol 接入信号 / 多持仓 top_n


### V32 影子独立仓位 + 组合已实现 vol (2026-07-21)

#### 实现
1. **影子 state 隔离**
   - `paths.shadow_state_file(name)` → `output/shadow_states/<name>.json`
   - 研究影子走独立 `load/execute/save`, **永不写** 生产 `模拟仓位.json`
   - CLI: `--shadow-exec` (dry-run 也写影子) / `--no-shadow-exec`
2. **组合已实现 vol**
   - `update_nav_history` 维护 `nav_history` + `port_rets`
   - `compute_target_exposure(..., port_rets=)`: vol/inv_vol **优先组合**, 不足回退基准
   - 报告标注 `vol_src=portfolio|bench`

#### 验证
- 三影子并行 dry-run + shadow-exec: 三文件独立存在
- 生产 STATE md5 **不变**
- 单元: 有 port_rets 时 `vol_src=portfolio`
- 当前市场趋势关: 暴露 0 (bear/空仓) 行为正确; 样本不足时 src=bench

#### 命令
```bash
# 生产不动, 只更新影子仓
python3 scripts/run_signal.py --dry-run --shadow-exec --shadow c01_q10_vt08_soft_oh38
# 正常日更 (生产+影子都写各自 state)
python3 scripts/run_signal.py --shadow c01_q10_vt08_soft_oh38
# 禁止影子写盘
python3 scripts/run_signal.py --dry-run --no-shadow-exec
```

#### 边界
- 生产默认策略仍 c01
- 同日多次运行 nav 点覆盖不重复累加 rets
- 组合 vol 需影子/生产持续跑日更后才从 bench 切到 portfolio
- 下一优先: 多持仓 top_n 信号 / 影子邮件摘要


### V33 多持仓 top_n 信号 + 影子摘要 (2026-07-21)

#### 实现
1. **`decide` / `execute` 多持仓路径** (`top_n>1`)
   - 目标集合 = 前 top_n 合格标; 等权拆分 `target_exposure`
   - `REBALANCE`: sell_codes / buy_codes / targets[]
   - state 写 `holdings[]`, 兼容 `holding=第一只`
   - `top_n=1` 生产路径不变
2. **研究配置** `config/c01_q10_vt08_soft_oh38_t2.json` (top_n=2)
3. **`scripts/shadow_summary.py`** 扫 `shadow_states/`

#### 验证
- 单元: multi 空仓建 2 只; trend off 清仓; top1 仍单标
- dry-run 三/四影子: 生产 state 不变; t2 独立文件
- 长代理全样本 (3bp): top1 sh1.36 dd-13.3%; top2 见命令输出 (对照, 未单独 14 项门禁)

#### 命令
```bash
python3 scripts/run_signal.py --dry-run --shadow-exec --shadow c01_q10_vt08_soft_oh38_t2
python3 scripts/shadow_summary.py
python3 scripts/run_backtest.py --strategy c01_q10_vt08_soft_oh38_t2 --count 640
```

#### 边界
- 生产默认 top_n=1 / c01 冻结
- 多持仓等权非回测精确复现 (无日内再平衡 inv_vol 细节)
- t2 仅研究影子; 晋级需另跑 validate_robust
- 下一优先: t2 长代理硬门禁 / 影子邮件附件摘要


### V34 t2 硬门禁否决 + 邮件影子摘要 (2026-07-21)

#### 长代理硬门禁 (`output/risk_audit/v34_long_gate.json`)
| 策略 | 状态 | sh | dd | 备注 |
|------|------|----|----|------|
| **vt08_soft_oh38** | **14/14 合格** | **1.36** | **-13.3%** | 研究主线 |
| vt09_oh35 | 14/14 合格 | 1.33 | -17.6% | 无状态机对照 |
| vt11 | 14/14 合格 | 1.22 | -22.2% | 防过拟合原锚 |
| **vt08_soft_oh38_t2** | **12/14 不合格** | 0.98 | -15.1% | **FAE<50% 否决** |

ETF soft: 四者均 5/5 合格 (短样本宽松不单独放行 t2).

#### 结论
1. **t2 不晋级**: 多持仓接线有用, 但长代理质量塌缩
2. 研究主线冻结: `c01_q10_vt08_soft_oh38` / `vt09_oh35` / `vt11`
3. 生产 c01 继续冻结

#### 邮件影子摘要
- `shadow_summary.py --text-out`
- `send_email.py --append-shadow [--shadow-names ...] [--dry-print]`

```bash
python3 scripts/validate_robust.py --preset long_proxy --strategies c01_q10_vt08_soft_oh38,c01_q10_vt08_soft_oh38_t2
python3 scripts/shadow_summary.py --text-out output/shadow_summary.txt
python3 scripts/send_email.py --append-shadow --dry-print
```

#### 边界
- 参数网格 / top_n 再扫优先级降为 0
- 下一优先: 日更影子积累 port_rets 后观察 vol_src=portfolio; 或生产只读监控面板


### V35 影子暖机 port_rets + 只读监控 (2026-07-21)

#### 实现
1. **`scripts/shadow_warmup.py`**
   - 长代理回测 equity → `shadow_states/*/port_rets` + `nav_history`
   - 默认 tail=120; `--reset` 重建; **永不写生产 state**
   - 暖机后清空 holdings (由信号日更接管), 只保留波动样本
2. **`scripts/shadow_monitor.py`**
   - 多影子只读: 趋势 / 暴露 / regime / vol_src / 账户
   - `--text-out` / `--json-out`

#### 验证 (本机)
| 影子 | rets | vol_src@bull | exp@bull | bt_sh |
|------|------|--------------|----------|-------|
| vt08_soft_oh38 | 119 | **portfolio** | 0.36 | 1.36 |
| vt09_oh35 | 119 | portfolio | 0.40 | 1.33 |
| vt11 | 119 | portfolio | 0.39 | 1.22 |
| t2 (对照) | 119 | portfolio | 0.22 | 0.98 |

- 生产 STATE md5 未变
- 当前趋势关: 监控暴露全 0 (bear/空仓正确)
- 暖机后 `compute_target_exposure` notes: **vol_target 用组合已实现波动**

#### 命令
```bash
python3 scripts/shadow_warmup.py --tail 120 --reset
python3 scripts/shadow_monitor.py --text-out output/shadow_monitor.txt
python3 scripts/run_signal.py --dry-run --no-shadow-exec --shadow c01_q10_vt08_soft_oh38
```

#### 边界
- 暖机 equity 来自长代理, 与 ETF 实盘波动有域差; 仅加速 vol_src 切换
- 暖机后持仓为空, 需信号日更重建影子仓
- 生产 c01 冻结; 研究主线不变 (soft_oh38 / oh35 / vt11)
- 下一优先: cron 串联 warmup(可选)→signal→monitor→email


### V36 日更编排 pipeline (2026-07-21)

#### 实现
1. **`scripts/run_pipeline.py`**
   - 步骤: `warmup? → signal → monitor → summary? → email?`
   - 默认生产 `c01` + 影子 `c01_q10_vt08_soft_oh38`
   - 无 SMTP 时 email **自动 skip** (不失败)
   - 写 `output/risk_audit/pipeline_last.json` + `output/logs/pipeline_*.log`
2. **`scripts/run_daily.sh`** 改为调 pipeline (兼容 ETF_ROOT / 旧路径)
3. **GHA `daily-signal.yml`** 单步跑 pipeline; artifact/commit 含 shadow_states 与 monitor

#### 验证 (本机 dry-run)
```
PIPELINE DONE ok=True
  OK signal / monitor / summary
  SKIP email (no SMTP)
```
- `vol_src=portfolio` (暖机后) 仍可见
- 生产策略未切换

#### 命令
```bash
# 本地日更预演
python3 scripts/run_pipeline.py --dry-run --steps signal,monitor,summary,email --append-shadow-email
# 实盘日更 (写生产仓+影子)
python3 scripts/run_pipeline.py --steps signal,monitor,email --append-shadow-email
# 可选暖机
python3 scripts/run_pipeline.py --warmup --warmup-tail 120 --steps warmup,signal,monitor
# cron
PIPELINE_WARMUP=0 bash scripts/run_daily.sh
```

#### 边界
- warmup 默认关 (长代理重); 需要时 `--warmup` 或 `PIPELINE_WARMUP=1`
- 邮件 Secrets 未配时 CI 不失败
- 生产 c01 冻结; 研究主线不变
- 参数网格继续不扫


### V37 交易日门控 + Pages 监控站 (2026-07-21)

#### 实现
1. **`calendar_util.is_trading_day` / `load_bench_calendar`**
   - 优先基准行情日历; 失败退回周一~五
2. **`run_pipeline.py --require-trading-day`**
   - 非交易日 skip: `signal` / `email` / `warmup`
   - 仍可跑 `monitor` / `summary` / `pages`
   - `--force-run` 忽略门控
3. **`scripts/build_pages.py`**
   - 静态站: 信号 / 监控 / 摘要 / JSON
   - pipeline `--pages-out` 或 steps 含 `pages`
4. **cron / GHA** 默认 `--require-trading-day` + 生成 `output/site`
5. Pages job 下载 signal artifact 后部署 (含 monitor)

#### 验证
- `is_trading_day('2026-07-18')=False` (六) / 周一 True
- pipeline dry-run: signal/monitor/summary/pages OK, email skip
- `output/site/{index,monitor,summary}.html` 生成
- yaml / bash -n 通过

#### 命令
```bash
python3 scripts/run_pipeline.py --dry-run --require-trading-day --pages-out output/site
python3 scripts/build_pages.py --out output/site
# 强制非交易日也跑信号
python3 scripts/run_pipeline.py --force-run --steps signal,monitor
```

#### 边界
- 基准日历失败时仅工作日近似, **不覆盖法定假日** (需未来节假日表)
- Pages 需仓库开启 GitHub Pages (Actions 源)
- 生产 c01 冻结; 研究主线不变
- 参数网格继续不扫


### V38 A股法定假日表门控 (2026-07-21)

#### 实现
1. **`config/cn_holidays.json`** — 2024-2027 休市日; `makeup` 默认空 (A股周末不开市)
2. **`calendar_util`**
   - `load_cn_holidays` / `is_cn_session_day` / `resolve_trading_day`
   - 门控真源: **行情日历覆盖范围内用 bench**; 范围外回退假日表
   - 无 bench: 假日表 + 周一~五
3. **`scripts/check_trading_day.py`** — CLI (非交易日 exit 2)
4. pipeline `--require-trading-day` 已用 `resolve_trading_day`

#### 验证
- 元旦/春节/五一/国庆等 closed → False (即使 weekday_only=True)
- 周末 False; 调休补班日 **不** 当作开市
- 未来 2026-10-01 → `cn_holidays_beyond_bench` False
- 今日 2026-07-21 → bench_calendar True
- pipeline `--require-trading-day` dry-run 正常通过

#### 命令
```bash
python3 scripts/check_trading_day.py
python3 scripts/check_trading_day.py --date 2026-10-01
python3 scripts/run_pipeline.py --require-trading-day --dry-run --pages-out output/site
```

#### 边界
- 假日表需每年按交易所公告增补
- 行情日历与假日表冲突时, **范围内以行情为准**
- 生产 c01 冻结; 参数网格不扫


### V39 研究主线健康回归 (2026-07-21)

参数网格已平台. 本轮交付 **一键回归** 锁定主线, 防接线/假日/暖机回退.

#### 实现
`scripts/research_healthcheck.py` 五检查:
1. **holiday** — 假日表用例 + resolve_trading_day
2. **configs** — c01 + 三主线可加载; 主线 research=true
3. **exposure** — 影子 rets≥20 且 vol_src=portfolio (必要时自动 warmup)
4. **pipeline** — dry-run signal/monitor/pages; 生产 state 不脏写
5. **long_anchor** — 长代理 sh/dd 门槛 (soft≥1.2/dd≤18%, oh35≥1.15, vt11≥1.1)

#### 验证 (本机)
```
OK holiday / configs / exposure / pipeline / long_anchor
HEALTHCHECK DONE ok=True ~12s
```
- soft_oh38 sh≈1.36 dd≈-13.3%
- oh35 / vt11 均过门槛

#### 命令
```bash
python3 scripts/research_healthcheck.py
python3 scripts/research_healthcheck.py --quick          # 无 long_anchor
python3 scripts/research_healthcheck.py --checks holiday,configs,pipeline
```

#### 边界
- 不扫新参数; 门槛只做回归锁
- t2 不在主线清单
- 生产 c01 冻结
- 建议 cron/周检: `research_healthcheck.py` 后 `run_pipeline.py`


### V40 周检编排 + 影子监控告警 (2026-07-21)

#### 实现
1. **`shadow_monitor` 告警阈值**
   - error: 缺 state / vol_src≠portfolio(样本足时)
   - warn: rets 过薄 / 趋势开暴露为0 / vol_scale 过低
   - `--fail-on-alert` exit 2; `--fail-on-warn` exit 3
   - 默认监控三主线 (不含已否决 t2)
2. **`scripts/run_weekly.py` / `run_weekly.sh`**
   - 默认: `research_healthcheck` → `shadow_monitor --fail-on-alert`
   - `--quick` / `--with-pipeline-dry`

#### 验证
- 监控: error=0 warn=0, vol_src=portfolio
- 周检 quick: healthcheck+monitor **ok=True**
- 单元: thin_rets=warn, vol_src_not_portfolio=error

#### 命令
```bash
python3 scripts/shadow_monitor.py --fail-on-alert
python3 scripts/run_weekly.py
python3 scripts/run_weekly.py --quick
# cron 示例 (周日 18:00 北京)
# 0 10 * * 0 cd /path/etf-rotation && bash scripts/run_weekly.sh
```

#### 边界
- 告警不自动改生产仓
- 趋势关时暴露0 不告警
- 生产 c01 冻结; 参数网格不扫


### V41 日更监控告警 + GHA 周检 (2026-07-21)

#### 实现
1. **pipeline** `--monitor-fail-on-alert` / `--monitor-fail-on-warn`
   - monitor 步骤透传 shadow_monitor 告警退出码
2. **run_daily.sh / daily-signal.yml** 默认开启 `--monitor-fail-on-alert`
3. **`.github/workflows/weekly-health.yml`**
   - 周日 10:00 UTC (北京 18:00) + workflow_dispatch
   - 跑 `run_weekly.py` (healthcheck + monitor 告警)
   - 上传 weekly/healthcheck/monitor 产物

#### 验证
- pipeline dry-run + fail-on-alert: **ok=True**, monitor error=0
- weekly/daily yaml 可解析; bash -n 通过

#### 命令
```bash
python3 scripts/run_pipeline.py --dry-run --monitor-fail-on-alert --require-trading-day --pages-out output/site
python3 scripts/run_weekly.py
# GHA: Weekly Research Health (手动 workflow_dispatch 可测)
```

#### 边界
- 告警失败会让日更/周检 job 红灯, 但不改生产仓逻辑
- 无 Secrets 时邮件仍 skip
- 生产 c01 冻结; 参数网格不扫


### V42 告警可见性 (Pages / 邮件 / pipeline JSON) (2026-07-21)

#### 实现
1. **pipeline_last.json** 增加 `alert_error_n` / `alert_warn_n` / `alerts[]`
2. **build_pages.py**
   - 导航「告警」页 `alerts.html` + `alerts.json`
   - 首页/监控页 badge: error/warn 计数
3. **send_email.py `--append-alerts`**
   - 正文附加告警汇总; error>0 时主题加 `ALERT×N`
   - pipeline `--append-shadow-email` 同时附加 shadow+alerts

#### 验证
- pipeline dry-run: `alerts error=0 warn=0`
- site 含 `alerts.html`; meta 含 alert 计数
- email dry-print 含「告警汇总」

#### 命令
```bash
python3 scripts/run_pipeline.py --dry-run --monitor-fail-on-alert --pages-out output/site --append-shadow-email
python3 scripts/send_email.py --dry-print --append-alerts --append-shadow
open output/site/alerts.html   # 或浏览器打开
```

#### 边界
- 无 SMTP 时邮件仍 skip, 但 dry-print 可预览
- 告警源优先 shadow_monitor.json
- 生产 c01 冻结; 参数网格不扫


### V43 运维闭环加固 (2026-07-21)

#### 实现
1. **pipeline dry-run 邮件预览**
   - 无 SMTP 时跑 `email_preview` (`send_email --dry-print` + shadow/alerts)
2. **healthcheck 增加 `monitor_alerts`**
   - 默认检查清单含告警 error=0
3. **`scripts/research_status.py`**
   - 一键只读面板: 交易日/配置/影子/告警/pipeline/health/weekly/site

#### 验证
- healthcheck --quick: 含 monitor_alerts **ok=True**
- pipeline dry-run: `email_preview` OK, 正文含告警汇总
- research_status 输出主线 rets=119 / alerts 0

#### 命令
```bash
python3 scripts/research_status.py
python3 scripts/research_healthcheck.py --quick
python3 scripts/run_pipeline.py --dry-run --append-shadow-email --monitor-fail-on-alert --pages-out output/site
```

#### 边界
- 状态面板只读, 不触发交易
- 生产 c01 冻结; 参数网格不扫


### V44 状态出口统一 (2026-07-21)

#### 实现
1. **pipeline 步骤 `status`**
   - 跑 `research_status.py` → `output/research_status.txt` + `risk_audit/research_status.json`
   - 顺序: … → status → pages (站点可嵌入最新面板)
2. **日更默认 steps**: `signal,monitor,email,pages,status` (cron/GHA 已改)
3. **Pages** 增加「状态」页 `status.html`
4. **周检** 末尾固定跑 status; artifact 含 research_status

#### 验证
- pipeline dry-run: signal/monitor/status/pages OK
- site 含 status.html; meta.has_status=True
- weekly --quick --skip-healthcheck: monitor_alert + status OK

#### 命令
```bash
python3 scripts/run_pipeline.py --dry-run --steps signal,monitor,status,pages --pages-out output/site
python3 scripts/research_status.py
# 站点
# output/site/status.html
```

#### 边界
- status 只读聚合, 不交易
- 生产 c01 冻结; 参数网格不扫


### V45 主线门禁加固 (2026-07-21)

#### 实现
1. **邮件附加 status**
   - `send_email.py --append-status` (缺产物时即时生成)
   - pipeline `append_shadow_email` 同步带上 `--append-status`
2. **healthcheck `etf_soft`**
   - 主线三策略走 `validate_robust --preset etf_soft`
   - 仅 **不合格/缺失** 失败 (观察可过, 对齐 promote 软门)
   - 默认检查含 etf_soft; `--quick` 跳过 etf_soft + long_anchor
3. **pipeline 健康检查** steps 含 status; 产物要求 status 文本或 status.html
4. 修复 healthcheck light/heavy 并行块语法

#### 验证
- email dry-print 含「研究状态面板」
- healthcheck --quick OK (holiday/configs/exposure/pipeline/monitor)
- healthcheck --checks etf_soft OK; 三主线 5/5 合格
- pipeline dry-run email,status OK

#### 命令
```bash
python3 scripts/send_email.py --append-shadow --append-alerts --append-status --dry-print
python3 scripts/research_healthcheck.py --quick
python3 scripts/research_healthcheck.py --checks etf_soft --skip-warmup
# 报告: output/risk_audit/health_etf_soft.json
```

#### 边界
- etf_soft 是短样本软门, 不是 long 14/14 替代
- 生产 c01 冻结; 不扫参数网格

### V46 soft 结构轴搜索 (2026-07-21)

#### 实现
1. 新搜索轴 (主线 soft+vt08+oh38 邻域):
   - `soft_exec` — min_hold / hyst / stop
   - `soft_vol` — vol_lookback / vol_wmin / vol_mode
   - `soft_struct` — soft_trend / state_pos / park_scale / 因子
2. run `20260721_205923`: 98 metrics; 去同构 shortlist 8 进双端门禁

#### 门禁
| id | long | etf_soft | 相对主线 |
|----|------|----------|----------|
| soft_exec_st6 | 14/14 | 合格 | 更弱 (sh1.25) |
| soft_exec_st5 | 14/14 | 合格 | 更弱 |
| soft_struct_st | 14/14 | **不合格** | soft_trend OOS 塌 |
| soft_struct_sp | 14/14 | 合格 | 更弱 (sh1.12) |
| soft_struct_st_sp | 14/14 | **不合格** | OOS 塌 |
| soft_vol_lb30_mn15_ewma | 14/14 | 合格 | 更弱 |
| soft_vol_lb20_mn25_std | 14/14 | 合格 | 近同构 |

**promote_ready: none**

#### 结构结论
- 执行摩擦: hyst 0.15–0.20 平台; stop=-0.08 锁定
- 波动估计: **lb=20 + std** 锁定; ewma/down/异 lb 全劣
- soft_trend/state_pos/换因子: 不提升主线, soft_trend 伤 ETF OOS
- 主线继续 `c01_q10_vt08_soft_oh38`

#### 命令
```bash
python3 scripts/search_dispatch.py --run-id auto --axes soft_exec,soft_vol,soft_struct --max-agents 2 --workers 2
# gate artifacts
# output/search_runs/20260721_205923/gate/
```

#### 边界
- 参数网格再扫收益递减; 优先运维/影子/成本
- 生产 c01 冻结


### V47 GitHub 社区新因子 (2026-07-21)

#### 调研来源
- [z-waterking/etf-rotation-backtester](https://github.com/z-waterking/etf-rotation-backtester): momentum / risk-adj / dual / min-vol
- [segobooking-finanz/momentum-sector-rotation](https://github.com/segobooking-finanz/momentum-sector-rotation): 12-1 月动量
- [AlphaRotationLab/US-ETF-Rotation-Model](https://github.com/AlphaRotationLab/US-ETF-Rotation-Model): 20d 动量 + 长均线 (已有趋势过滤)
- A 股轮动文多仍是纯动量, 无新可迁移价量结构

#### 实现
`factors.py` 新增键 (close/volume only):
`m60_skip20`, `m20_skip5`, `dual_m60`, `low_vol`, `mom_vol60`, `m120`, `rsi14`, `rsi_mid`
搜索轴: `factor_gh` (12 packs × softvt8/vt09oh35)

#### 结果 run `20260721_221108`
| pack | 最佳 sh | 结论 |
|------|---------|------|
| c01w (对照) | 1.36 | 主线同构 |
| lowvol_m20 | 0.76 | 淘汰 |
| dual_eff | 0.75 | 淘汰 |
| rsi / m120 / skip / dual60 / mv60 | ≤0.43 或负 | 淘汰 |

**promote_ready: none**

#### 结论
- 美股社区月频 12-1 / dual / min-vol **不能**直接改善 A 股日频行业轮动
- 主线仍 `eff+mtf` + soft + vt08 + oh38
- 因子代码保留供后续对照, 不进生产/影子默认

#### 边界
- 无外部基本面/资金流数据; 未引入需付费字段
- 生产 c01 冻结


### V48 日更体验: Pages 面板 + HTML 邮件 (2026-07-21)

#### 实现
1. **Pages 可视化面板** (`scripts/build_pages.py`)
   - 首页 `index.html` 改为卡片 KPI / 动作 / 检查清单 / TOP / 影子监控
   - 导航: 面板 · 信号原文 · 状态 · 监控 · 告警 · 摘要 · JSON
   - 深色响应式; 数据源 latest.json + shadow_monitor.json + research_status.json
2. **邮件 HTML 美化** (`scripts/send_email.py`)
   - 默认 multipart: HTML + 纯文本
   - 头部动作卡 / KPI / 检查表 / TOP / 影子表
   - `--plain-only` 可回退; dry-print 写 `output/email_preview.html`
   - 可选 `MAIL_PAGES_URL` 内嵌面板链接
3. **GHA**: commit 含 `output/site` 与 research_status

#### 验证
- `python3 scripts/build_pages.py --out output/site` → dashboard=True
- `python3 scripts/send_email.py --append-shadow --append-alerts --append-status --dry-print`
- 产物: `output/site/index.html`, `output/email_preview.html`

#### 用法
```bash
python3 scripts/build_pages.py --out output/site
# 本地预览
# open output/site/index.html
python3 scripts/send_email.py --append-shadow --append-alerts --append-status --dry-print
# 可选 Secrets: MAIL_PAGES_URL=https://<user>.github.io/<repo>/
```

#### 边界
- 面板只读, 不交易; 生产 c01 冻结
- Pages 需仓库 Settings → Pages → Source = GitHub Actions

### V49 净值曲线 + 动作时间线 (2026-07-21)

#### 实现
1. **Pages**
   - 影子净值归一化 SVG (soft_oh38 / vt09_oh35 / vt11)
   - 近 N 日动作时间线
2. **动作历史**
   - `write_latest_json` 自动追加 `output/action_history.jsonl` (同日覆盖)
   - 面板无 jsonl 时回退解析 `etf信号_*.txt`
3. **邮件**
   - HTML 增加「近几日动作」表

#### 验证
- build_pages 含 svg + timeline
- send_email dry-print 含近几日动作
- 预览: `output/site/index.html`, `output/email_preview.html`

#### 边界
- 净值来自 shadow warmup 后的 nav_history
- 生产 c01 冻结; 面板只读


### V50 可用性入口 + 跨资产宇宙 (2026-07-21)

#### 易用性
1. `scripts/etf.py` 统一 CLI: doctor/status/daily/preview/pages/email-preview
2. `scripts/doctor.py` 一键体检 (配置/池/影子/站点/SMTP)
3. `research_status` 增加「下一步」提示
4. README 日常入口改写

#### 有效收益 · universe_park
1. 新轴 `universe_park` (park/extra/prefer)
2. **关键修复** (否则短历史黄金/纳指会把 10y 样本压成 ~2y):
   - backtest 公共日期忽略短历史/optional 代码
   - park/短历史默认不进进攻打分
   - validate/search 加载 shortlist 的 extra/park 代码
3. 双端门禁 (修复后):

| id | long | etf_soft | 相对主线 |
|----|------|----------|----------|
| extra_gold_nq | 14/14 sh**1.47** dd**-12.7%** | 合格 | **更优** |
| extra_gold | 14/14 sh1.39 dd-12.7% | 合格 | 略优 |
| park_* | 14/14 近同构或更弱 | 合格 | 不 promote |
| soft_trend park | 更弱 | 合格 | 不 promote |
| prefer_bench | 明显更弱 | — | 淘汰 |

4. 研究配置:
   - `config/c01_q10_vt08_soft_oh38_xgn.json` (黄金+纳指)
   - `config/c01_q10_vt08_soft_oh38_xg.json` (仅黄金)

#### 说明
- 生产 ETF 池已含黄金/纳指 → etf_soft 与主线近同构 (不回归)
- 长代理池本无跨资产 → extra_universe 增益主要体现在 long proxy 与未来扩池
- 生产 c01 仍冻结; 影子默认仍 soft_oh38, xgn 作研究候选

#### 用法
```bash
python3 scripts/etf.py doctor
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py preview
# 研究影子可改 --shadow c01_q10_vt08_soft_oh38_xgn
```


### V51 xgn 主线接入 + 可用性收口 (2026-07-22)

#### 实现
1. `etf_rotation/research_mainline.py` 统一主线名单:
   - 信号默认影子 `c01_q10_vt08_soft_oh38_xgn`
   - 监控/暖机含 soft_oh38 / xgn / oh35 / vt11
   - LONG_GATES 含 xgn (sh≥1.3, dd≤16%)
2. doctor / status / pipeline / daily.sh / GHA 对齐默认影子
3. shadow_warmup / long_anchor 加载 extra_codes (黄金/纳指)
4. Pages 净值图含 xgn
5. xgn 暖机完成: bt_sh=1.47, vol_src=portfolio

#### 验证
- doctor PASS (xgn state 已有)
- healthcheck long_anchor OK (含 xgn)
- warmup 4 影子 OK, 生产 state 未改

#### 用法
```bash
python3 scripts/etf.py doctor
python3 scripts/etf.py daily --dry-run          # 默认 shadow=xgn
python3 scripts/shadow_warmup.py --reset        # 默认含 xgn
python3 scripts/etf.py preview
```

#### 边界
- 生产 c01 仍冻结
- xgn 为研究影子; 实盘晋级仍需观察期


### V52 信号 extra 对齐 + 暖机净值修复 (2026-07-22)

#### 真有效修复
1. **信号侧 `extra_universe`/`park_assets` 取数与打分宇宙** 与回测对齐  
   (`etf_rotation/signal.py`) — 否则 xgn 在 live 信号里可能缺跨资产
2. **暖机空仓 cash 对齐净值** (`shadow_warmup.py`)  
   修复信号日把暖机曲线重置为 10 万的问题
3. **空仓信号保留暖机 NAV** (`signal.py` update_nav 路径)
4. `run_signal.py` 默认影子 xgn; 补回误删的 `--bars`

#### 可用性
- `shadow_monitor` 默认 MONITOR_SHADOWS (含 xgn)
- `cost_sensitivity` 默认含 xgn, 并加载 extra_codes

#### 成本敏感 (长代理, 3bp)
| 策略 | sh / dd |
|------|---------|
| c01 | 0.52 / -33% |
| soft_oh38 | 1.36 / -13.3% |
| **xgn** | **1.47 / -12.7%** |
| vt09_oh35 | 1.33 / -17.6% |
| vt11 | 1.22 / -22.2% |

20bp 下 xgn 仍 sh≈1.10, 优于主线与 c01.

#### 验证
- warmup xgn cash=total_value
- signal --dry-run --shadow-exec: 影子账户保持 ~91 万
- monitor 4 影子 error=0
- etf.py daily --dry-run 全绿

#### 边界
- 生产 c01 冻结; 当前趋势关空仓
- xgn 为研究影子默认


### V53 dry-run 写影子 + 主线对照 (2026-07-22)

#### 真有效 / 可用性
1. **pipeline dry-run 默认 `--shadow-exec`**  
   只禁生产仓, 研究影子照常更新 (修正帮助文案与实现不一致)
2. **`scripts/shadow_compare.py` + `etf.py compare`**  
   一表看 sh/dd/净值/gate/3bp 成本, 标 SIGNAL 默认
3. **status 面板嵌入对照摘要**
4. 全部主线影子 **cash=NAV** 暖机对齐 (消除 CASH≠NAV)

#### 验证
- compare: xgn sh1.47 dd-12.7% SIGNAL OK; soft_oh38 1.36/-13.3%
- daily --dry-run 全绿, signal 带 shadow-exec
- xgn 日更后净值保持 ~91 万

#### 用法
```bash
python3 scripts/etf.py compare
python3 scripts/etf.py daily --dry-run    # 更新影子, 不改生产
python3 scripts/etf.py status
```

#### 边界
- 生产 c01 冻结
- 对照 sh/dd 来自暖机摘要, 非 live 成交账本


### V54 日更 compare + 对照页 + cash 体检 (2026-07-22)

#### 实现
1. pipeline 新步骤 **`compare`** (在 status/pages 前)
2. 默认日更 steps: `signal,monitor,email,compare,pages,status`
   - `etf.py daily` / `run_daily.sh` / GHA 对齐
3. Pages 导航 **对照** → `compare.html` (主线 sh/dd/净值/gate/3bp)
4. doctor 检查影子 **cash=NAV**, 并登记 shadow_compare/etf 脚本

#### 验证
- daily --dry-run 含 compare, 全绿
- site/compare.html 有主线对照表
- doctor 影子无 CASH≠NAV

#### 用法
```bash
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py compare
# 面板: output/site/compare.html
```

### V55 邮件/体检/预览对齐对照 (2026-07-22)

#### 实现
1. `send_email.py --append-compare` (plain+HTML 主线对照块; 缺产物时自动跑 shadow_compare)
2. pipeline 邮件默认附加 compare (与 shadow/alerts/status 一并)
3. healthcheck pipeline steps: `signal,monitor,compare,status,pages` + 产物存在检查
4. `etf.py preview` 先 compare 再 status/pages; email-preview append-all 含 compare

#### 验证
- email dry-print 含「主线对照」表 (xgn SIGNAL sh1.47)
- healthcheck ok=True, pipeline 含 compare
- preview 写出 compare.html

#### 用法
```bash
python3 scripts/etf.py email-preview --append-all
python3 scripts/etf.py preview
python3 scripts/research_healthcheck.py
```

### V56 主线影子批量日更成交 (2026-07-22)

#### 背景
- 暖机后 holdings 清空是设计; 此前日更只成交 **1 个** 信号影子, 其余主线对照只盯市不进场.
- 当前大盘 MA20 下趋势关 → 全主线合法空仓 (非 bug).

#### 实现
1. `signal._process_research_shadow` + `_mark_nav` (支持 multi holdings 盯市)
2. `run_signal(..., extra_shadows=)`: 主线 MONITOR 批量 decide/execute (共用 market)
3. CLI: `--extra-shadows` 默认 MONITOR_SHADOWS; `--no-extra-shadows` 仅主影子
4. pipeline signal 传 `--extra-shadows {args.shadows}`
5. latest.json `shadow.mainline[]` 含各影子 action/持仓/净值
6. compare/status/Pages 展示 live 持仓与最近成交

#### 验证
- dry-run --shadow-exec: 4 主线 last_update 同步, mainline 4 行, 趋势关空仓
- doctor PASS; compare 表含 holdings/last_trade 字段
- 生产 c01 仍冻结, dry-run 不改生产仓

#### 用法
```bash
python3 scripts/run_signal.py --dry-run --shadow-exec   # 默认批量主线
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py compare
```



### V57 live 段真实收益口径 (2026-07-22)

#### 问题
- `return_pct` 含暖机回测权益 (如 +813%), 不是日更真实有效收益
- 趋势关空仓时无法区分「策略没跑」与「live 没赚钱」

#### 实现
1. `portfolio.apply_live_metrics`: 锚点=暖机末净值 (`live_anchor_nav`/`live_start_date`)
2. `live% = nav/anchor - 1`; 写 `state.live` (n_rets/sharpe/trades/holdings)
3. 暖机结束写入锚点; 信号日更后刷新 live
4. `scripts/shadow_live.py` + `etf.py live` (可 `--write-states` 回填旧 state)
5. compare/status/Pages 展示 **live%**

#### 验证
- live 表: 4 主线 live=+0.00% (趋势关空仓, 锚点=当前净值) — 真实有效
- compare/status 含 live%; latest.mainline 含 live
- doctor 登记 shadow_live.py

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py compare
# live% 才是日更有效收益; 暖机 sh 仅研究对照
```




### V58 日更/邮件/面板对齐 live (2026-07-22)

#### 实现
1. pipeline 新步骤 **`live`** (compare 后): `shadow_live.py --write-states`
2. 默认日更 steps: `signal,monitor,email,compare,live,pages,status`
   - `etf.py daily` / `run_daily.sh` / GHA 对齐
3. 邮件 `--append-live` (plain+HTML); pipeline 默认附加
4. 面板 KPI **影子 live%**; 研究影子卡显示 live
5. healthcheck pipeline 含 live + 产物检查; doctor 常用含 `etf live`

#### 验证
- daily --dry-run: live 步骤 OK, pipeline ok=True
- email_preview 含 LIVE 块; site 有 live% KPI
- 趋势关 → live=+0.00% (真实有效)

#### 用法
```bash
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py live
# 邮件预览含 live
python3 scripts/etf.py email-preview
```


### V59 monitor/Pages LIVE 面 (2026-07-22)

#### 实现
1. `shadow_monitor` 输出 live% / anchor / from (只读补算)
2. Pages 导航 **LIVE** → `live.html`; 监控表/卡片含 live%
3. `etf preview` 串联 live 产物 + 打印 live.html 路径
4. status 下一步含 LIVE; 修 site 存在判断 (built_at)
5. GHA artifact/commit 含 shadow_live.txt/json

#### 验证
- monitor 文本含 live=+0.000%
- site/live.html + nav LIVE
- preview 打开 LIVE 路径
- doctor PASS

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py preview
# 面板: output/site/live.html
```

### V60 邮件/体检/摘要 live 防误读 (2026-07-22)

#### 问题
- 邮件 KPI「收益」展示全样本 return_pct (可 +800%), 易误读为日更真实收益
- summary/doctor 未标 live%, 与面板口径不一致

#### 实现
1. 邮件 KPI 改为 **影子 live%** (state.live / live_return_pct)
2. `shadow_summary` 并列 live / 全样本
3. doctor: 影子行带 live%; 检查 site/live.html + shadow_live 产物; 缺锚点 WARN
4. status 嵌入 **主线 LIVE** 摘要块

#### 验证
- email_preview 含「影子 live%」
- doctor PASS, site_live/live_artifacts OK
- summary/status 含 live

#### 用法
```bash
python3 scripts/etf.py email-preview
python3 scripts/etf.py doctor
python3 scripts/shadow_summary.py
```



### V61 live vs 基准超额 (2026-07-22)

#### 动机
- 趋势关空仓时 live%=0 无法单独判断「避险是否有效」
- 需要相对沪深300 同期的 **excess%** 作为真实有效对照

#### 实现
1. `shadow_live`: `bench%` / `xs% = live% - bench%` (start 日收盘为锚)
2. Pages `live.html` 与面板 KPI **超额 xs%**
3. `site_meta.has_live` / `has_compare`
4. 周检 dry pipeline steps 含 compare+live; weekly artifact 含 live 产物

#### 验证
- live 表含 bench%/xs%
- site KPI 超额; site_meta has_live=True
- 当前 start=末交易日 → xs=0 (正常; 下一交易日若空仓且基准动则 xs≠0)

#### 用法
```bash
python3 scripts/etf.py live
# excess>0 且空仓 = 避险跑赢基准
```


### V62 excess 全链路展示 (2026-07-22)

#### 实现
1. 邮件 KPI: **影子 live%** + **超额 xs%** (基准同期)
2. compare 表: live% / xs% 列 (接 shadow_live.json)
3. monitor / status: live + xs
4. doctor live_artifacts 摘要 SIGNAL live/xs/bench

#### 验证
- compare/monitor/status/email/doctor 均含 xs
- 当前 start=末交易日 → xs=0 (正常)

#### 读法
- 空仓 + 基准跌 → xs>0: 避险有效
- 空仓 + 基准涨 → xs<0: 踏空成本


### V63 summary/对照/体检接 xs (2026-07-22)

#### 实现
1. `shadow_summary` 并列 live / xs (读 shadow_live.json)
2. Pages `compare.html` 表增加 **xs%** 列
3. healthcheck 新检查 **`live`** (SIGNAL live/xs 字段; 默认纳入; 不因 xs=0 失败)

#### 验证
- summary 主线 xs=+0.00%; t2 无 live 产物 → xs=—
- compare.html 含 xs%
- `research_healthcheck --checks live` ok=True

#### 用法
```bash
python3 scripts/shadow_summary.py
python3 scripts/research_healthcheck.py --checks live --quick
```


### V64 etf summary + 日更接入 (2026-07-22)

#### 实现
1. `etf.py summary` → shadow_summary (text+json, 含 live/xs)
2. 默认日更 steps 含 **summary**:
   `signal,monitor,email,compare,live,summary,pages,status`
   (`etf daily` / `run_daily.sh` / GHA)
3. pipeline summary 写 `risk_audit/shadow_summary.json`
4. Pages 监控表加 **xs%**; doctor 登记 summary 脚本/产物

#### 验证
- etf summary 输出 live/xs
- daily --dry-run 含 summary 全绿
- doctor PASS

#### 用法
```bash
python3 scripts/etf.py summary
python3 scripts/etf.py daily --dry-run
```


### V65 preview/status/GHA 对齐 summary (2026-07-22)

#### 实现
1. 邮件 `--append-shadow` 写回 `shadow_summary.txt/json` (与 etf summary 一致)
2. `etf preview` 串联 summary, 打印 summary.html 路径
3. status 嵌入 **影子摘要** 块 + 下一步 `etf summary`
4. GHA artifact/commit 含 `shadow_summary.json` (修 commit if/else)

#### 验证
- preview 含摘要路径; status 有影子摘要 live/xs
- email-preview 含 live/xs 摘要
- doctor PASS summary_artifacts

#### 用法
```bash
python3 scripts/etf.py preview
python3 scripts/etf.py summary
```


### V66 etf monitor + 周检/体检对齐 summary (2026-07-22)

#### 实现
1. `etf.py monitor` → shadow_monitor (text+json, 含 live/xs)
2. 周检 dry / healthcheck pipeline steps:
   `signal,monitor,compare,live,summary,status,pages`
3. healthcheck pipeline 校验 summary 产物
4. help / doctor / status 下一步含 monitor

#### 验证
- etf monitor 输出 live/xs, 趋势关空仓
- doctor PASS; help 含 monitor

#### 用法
```bash
python3 scripts/etf.py monitor
python3 scripts/etf.py daily --dry-run
```


### V67 etf warmup + preview 监控 (2026-07-22)

#### 实现
1. `etf.py warmup` → shadow_warmup (默认 tail=120, 可 --reset)
2. `etf preview` 串联 **monitor** + compare/live/summary
3. doctor 检查 **monitor_artifacts**; 常用含 warmup
4. weekly GHA artifact 含 shadow_summary

#### 验证
- help 含 warmup/monitor
- preview 含监控路径与刷新
- doctor PASS

#### 用法
```bash
python3 scripts/etf.py warmup --tail 120
python3 scripts/etf.py monitor
python3 scripts/etf.py preview
```


### V68 live 薄样本提示 + warmup 体检 (2026-07-22)

#### 问题
- 暖机末≈最新交易日时 live/xs 常为 0, 易误读为策略无效
- doctor 未提示 live 样本天数, 也未登记 warmup 产物

#### 实现
1. `shadow_live`: `days_live` / `thin_live` (<5 日标 THIN)
2. live 文本/Pages 标注 THIN; doctor live_artifacts 薄样本 WARN
3. doctor: `warmup_artifacts` + 脚本 `shadow_warmup.py`/`shadow_monitor.py`
4. preview 打开 monitor.html; status 下一步含 warmup

#### 验证
- live 表 THIN days=1
- doctor PASS (live 薄样本 WARN, 非失败)
- site/live.html 含 THIN

#### 读法
- THIN: 等下一交易日再看 xs 才有意义
- 趋势关空仓 + 基准跌 → xs>0 才是避险有效


### V69 etf check + THIN 全表面 (2026-07-22)

#### 实现
1. `etf.py check` → research_healthcheck (默认 --quick; `--full` 完整)
2. compare note 标 **THIN**; 说明样本<5日
3. 邮件 KPI: live% 后附 THIN, 基准旁 Lrets
4. doctor 常用含 check

#### 验证
- etf check --checks holiday,configs,live ok
- compare/email 含 THIN
- doctor PASS

#### 用法
```bash
python3 scripts/etf.py check
python3 scripts/etf.py check --full
python3 scripts/etf.py compare
```


### V70 summary/monitor/status THIN 对齐 (2026-07-22)

#### 实现
1. `shadow_summary` / `shadow_monitor`: 输出 **THIN** + Lrets
2. status 下一步含 **etf check**
3. healthcheck `live` 报告 `days_live` / `thin_live` (不因 THIN 失败)

#### 验证
- summary/monitor 主线 live=… THIN Lrets=1
- check --checks live ok + days_live/thin_live
- status 下一步含健康检查

#### 用法
```bash
python3 scripts/etf.py summary
python3 scripts/etf.py monitor
python3 scripts/etf.py check --checks live
```


### V71 面板/监控/status THIN KPI (2026-07-22)

#### 实现
1. 面板 KPI **影子 live%** 附 THIN + Lrets
2. 监控页 live 列标 THIN
3. status 影子行: live=… THIN · Lrets

#### 验证
- index.html KPI `+0.00% THIN · Lrets=1`
- monitor.html 含 THIN
- status 影子行 THIN

#### 用法
```bash
python3 scripts/etf.py preview
# 面板: output/site/index.html
```

### V72 etf today 一页日报 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py today` — 动作 + SIGNAL live/xs/THIN + monitor/doctor + 下一步
2. 落盘 `output/today.txt` · `output/risk_audit/today.json`
3. help / doctor / status 入口指向 today
4. doctor 可选 `today_artifacts`

#### 验证
- today 输出 live THIN + Lrets=1
- doctor PASS 含 today_artifacts
- help 首条 today

#### 用法
```bash
python3 scripts/etf.py today
# 只打印不写盘: --no-write
```

### V73 today 进日更 + 邮件主题 live/xs (2026-07-22)

#### 实现
1. 默认日更 steps 末尾加 **today**
   - `etf daily` / `run_daily.sh` / GHA / healthcheck / weekly dry
2. pipeline 新步骤 `today` (status 后, pages 前)
3. `etf preview` 链 today, 打印今日路径
4. 邮件主题: `… | live+x.x% xs+x.x% THIN`
5. healthcheck 校验 today 产物

#### 验证
- subject: `ETF轮动信号 | ⚪ 空仓观望 | live+0.0% xs+0.0% THIN`
- pipeline `--steps today` exit=0
- today.txt / today.json 存在

#### 用法
```bash
python3 scripts/etf.py today
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py email-preview
```

### V74 status live 列 + today 页/邮件 (2026-07-22)

#### 实现
1. 状态页影子表: **live% / xs% / THIN / Lrets**
2. Pages 导航 **今日** → `today.html`
3. 邮件 `--append-today`; pipeline/email-preview 默认附加
4. 日更 steps 顺序: `…,status,today,email,pages` (email 在 live/today 后)

#### 验证
- status.html 影子 live `+0.00% THIN`
- today.html 存在; site_meta `has_today=True`
- email 正文含「今日速览」+ 主题 live/xs/THIN

#### 用法
```bash
python3 scripts/etf.py today
python3 scripts/etf.py pages
# output/site/today.html · status.html
python3 scripts/etf.py email-preview
```


### V75 doctor 站点页 + today 产物归档 (2026-07-22)

#### 实现
1. doctor 检查 `site_today` / `site_status` (缺页 WARN)
2. GHA daily artifact + commit: `today.txt` / `risk_audit/today.json`
3. weekly artifact 含 today 产物

#### 验证
- doctor PASS: site_today / site_status / today_artifacts OK

#### 用法
```bash
python3 scripts/etf.py doctor
# 站点: output/site/today.html · status.html
```

### V76 preview 今日页 + healthcheck today (2026-07-22)

#### 实现
1. `etf preview` 打印 `today.html` / `status.html` 打开路径
2. 面板 hero 链到 **今日速览**
3. healthcheck 新检查 **`today`** (默认集含; 缺则现算; THIN 不失败)

#### 验证
- `research_healthcheck --checks today` OK (thin_live=true 仍过)
- index.html 含「今日速览」链接

#### 用法
```bash
python3 scripts/etf.py preview
python3 scripts/etf.py check --checks today
# 打开: output/site/today.html
```

### V77 status Lrets 纠偏 + 嵌 today (2026-07-22)

#### 实现
1. 状态页影子表 **Lrets** 只显示 `days_live` (不再回落到 rets=120)
2. `research_status` 文本嵌入 **今日速览** 块
3. help 示例加 `check --checks today`

#### 验证
- status.html Lrets=1 (非 120)
- research_status.txt 含「今日速览」与 THIN

#### 用法
```bash
python3 scripts/etf.py status
python3 scripts/etf.py pages
# 状态页: output/site/status.html
```

### V78 status 自动 today + site_meta (2026-07-22)

#### 实现
1. `research_status` 缺 today 产物时自动 `etf today`
2. status 文本 site 行: `today=/live=/compare=`
3. doctor 检查 `site_meta.has_today`

#### 验证
- 删 today 后跑 status → 自动重建 today.txt/json
- doctor PASS: `site_meta has_today=True`

#### 用法
```bash
python3 scripts/etf.py status
python3 scripts/etf.py doctor
```

### V79 compare THIN + latest.signal_live (2026-07-22)

#### 实现
1. 对照页 live% 标 **THIN**; rets 列改为 **Lrets**
2. `shadow_live` 回写 `latest.json`:
   - 顶层 `signal_live` (live/xs/days/thin)
   - `shadow.mainline[]` 同步 excess/days/thin

#### 验证
- `etf live` → `PATCHED latest.json signal_live`
- compare.html: `+0.00% THIN` · Lrets=1

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py pages
# output/site/compare.html · output/latest.json
```

### V80 统一读 latest.signal_live (2026-07-22)

#### 实现
1. `etf today` / 面板 KPI / 邮件主题+HTML: **优先** `latest.signal_live`
2. 回退 `shadow_live.json` SIGNAL 行
3. doctor 检查 `latest_signal_live` (THIN 仅 WARN)

#### 验证
- today / index / email 主题均 THIN
- doctor PASS + `latest_signal_live` WARN(THIN)
- 去掉 signal_live 后 today 仍可从 shadow_live 回退

#### 用法
```bash
python3 scripts/etf.py live    # 回写 latest.signal_live
python3 scripts/etf.py today
python3 scripts/etf.py doctor
```

### V81 latest.txt 有效收益块 + status LIVE (2026-07-22)

#### 实现
1. `shadow_live` upsert `latest.txt` **有效收益 (SIGNAL live)** 块
2. 二次 live 不重复追加
3. `research_status` 主线 LIVE 优先 `latest.signal_live`

#### 验证
- latest.txt 含 live/xs/THIN · count_blocks=1
- status 主线 LIVE 来自 signal_live

#### 用法
```bash
python3 scripts/etf.py live
# 看: output/latest.txt 末尾有效收益块
python3 scripts/etf.py status
```

### V82 ensure live + healthcheck/doctor 链路 (2026-07-22)

#### 实现
1. `research_status` 缺 `latest.signal_live` 时自动 `shadow_live`
2. healthcheck `check_live` 要求并回写 `latest.signal_live` + 记录 txt 块
3. doctor 检查 `latest_txt_live` (THIN 仅 WARN)

#### 验证
- 删 signal_live / txt 块后 `etf status` 自动恢复
- healthcheck live OK (`latest_signal_live=true`)
- doctor PASS + latest_txt_live WARN(THIN)

#### 用法
```bash
python3 scripts/etf.py status
python3 scripts/etf.py check --checks live
python3 scripts/etf.py doctor
```

### V83 today 自愈 live + signal 保留 signal_live (2026-07-22)

#### 实现
1. `etf today` 缺 live 产物时自动 `shadow_live`
2. `run_signal` 写 latest 时 **保留** 上一轮 `signal_live`
3. signal 重写 `latest.txt` 后补回有效收益块

#### 验证
- 删 signal_live + shadow_live 后 `etf today` 自动恢复
- preserve: 覆盖 latest 后 patch 回 txt 块

#### 用法
```bash
python3 scripts/etf.py today
# signal 后仍保留 live 摘要, 完整日更仍以 live 步为准
```

### V84 时间线 live + 信号页高亮 (2026-07-22)

#### 实现
1. `action_history` 记录 `signal_live` (live/xs/days/thin)
2. 面板时间线展示 live/xs/THIN
3. `signal.html` 高亮有效收益块
4. doctor / status / help 入口补 `check --checks live|today`

#### 验证
- action_history 末行含 signal_live
- signal.html 含「有效收益 (SIGNAL live)」卡片
- 时间线可见 live/xs

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py pages
# output/site/signal.html · index.html 时间线
```

### V85 live 同步时间线 + 信号入口 (2026-07-22)

#### 实现
1. `shadow_live` patch 后 `append_action_history` (时间线 live/xs 随 live 刷新)
2. `etf preview` 打印 `signal.html` 打开路径
3. 面板 hero 链 **信号原文**
4. GHA daily 打印有效收益块 + `signal_live` JSON

#### 验证
- live 后 action_history.signal_live 非脏数据
- index 含 signal.html 链接
- preview 含「打开信号」

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py pages
# 时间线 / signal.html / GHA 日志有效收益段
```

### V86 status 首屏 live + 时间线排序 (2026-07-22)

#### 实现
1. `research_status` 首屏 **有效收益 (SIGNAL)** (live/xs/THIN)
2. `action_history` 按日期排序写入
3. live 刷新 **最近一日** 的 signal_live (不依赖偏旧 latest.time)
4. doctor 检查最近带 live 的 action 日
5. weekly GHA 打印 signal_live / 有效收益块

#### 验证
- status 顶部即 live/xs
- action_history 日期有序; 最近日有 signal_live
- doctor PASS + action_history THIN WARN

#### 用法
```bash
python3 scripts/etf.py status
python3 scripts/etf.py live
python3 scripts/etf.py doctor
```

### V87 status 页 live KPI + latest 过旧告警 (2026-07-22)

#### 实现
1. `status.html` KPI: **SIGNAL live% / xs%** (+ THIN/Lrets)
2. status 文本 / today / doctor: **latest 过旧** 告警 (信号日 < 交易日)
3. doctor `latest_freshness` (WARN, 不失败)

#### 验证
- status 首屏: 过旧告警 + live/xs
- status.html: SIGNAL live% KPI + 过旧提示
- doctor PASS + latest_freshness WARN

#### 用法
```bash
python3 scripts/etf.py status
python3 scripts/etf.py today
python3 scripts/etf.py doctor
# 过旧时: python3 scripts/etf.py daily --dry-run
```

### V88 过旧优先 daily + 面板/邮件提示 (2026-07-22)

#### 实现
1. status 下一步: latest 过旧时 **优先** `daily --dry-run`
2. 面板 index 过旧横幅
3. 邮件 HTML + 纯文本 过旧提示

#### 验证
- status 下一步首条 daily --dry-run (优先)
- index.html / email 含过旧提示

#### 用法
```bash
python3 scripts/etf.py status
# 过旧时:
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py pages
python3 scripts/etf.py email-preview
```

### V89 etf refresh + latest.time 用本次运行时 (2026-07-22)

#### 实现
1. `etf refresh` = `daily --dry-run` (一键刷过旧信号, 不改生产仓)
2. today / status / doctor 过旧时优先提示 **refresh**
3. `signal` 写 `latest.time` 用 **本次 now**, 不再沿用旧 last_update

#### 验证
- refresh 后 `latest.time=2026-07-22`, 不再过旧
- doctor `latest_freshness` OK
- pipeline DONE ok=True

#### 用法
```bash
python3 scripts/etf.py refresh   # 过旧时优先
python3 scripts/etf.py status
python3 scripts/etf.py doctor
```

### V90 过旧提示统一 etf refresh (2026-07-22)

#### 实现
1. 面板 / status 页 / 邮件 / today / doctor 过旧提示统一 **`etf refresh`**
2. README 增加 refresh 入口

#### 验证
- 人为过旧后: status/index/email 均指向 refresh
- 恢复 latest.time 后正常

#### 用法
```bash
python3 scripts/etf.py refresh   # 过旧时优先
python3 scripts/etf.py status
```

### V91 行情截至 asof + DATA_LAG (2026-07-22)

#### 实现
1. `resolve_trading_day`: `data_asof` / `data_lag`
2. signal 写 nav/live 用 **行情截至日**, 不用 wall-clock
3. `update_nav_history` 截断超前虚假日期点
4. status / today / doctor 展示行情滞后 (DATA_LAG)
5. latest.json 带 `market_asof`

#### 验证
- 行情截至 2026-07-21, wall 2026-07-22 → DATA_LAG
- nav 无 07-22 虚假点; live Lrets 以 asof 为准
- doctor PASS + trading_day DATA_LAG WARN

#### 用法
```bash
python3 scripts/etf.py refresh
python3 scripts/etf.py status   # 看 行情截至 / DATA_LAG
python3 scripts/etf.py today
```

### V92 全路径展示 DATA_LAG (2026-07-22)

#### 实现
1. 面板 index / status 页: **DATA_LAG** 横幅 + asof
2. 邮件 HTML/纯文本: 行情滞后提示 + 头栏行情截至
3. healthcheck 新检查 **`data_asof`** (lag 不失败)
4. GHA daily 打印 market_asof / trading_day data_lag

#### 验证
- index/status/email 含 DATA_LAG 与 行情截至 2026-07-21
- `check --checks data_asof` OK

#### 用法
```bash
python3 scripts/etf.py pages
python3 scripts/etf.py check --checks data_asof
python3 scripts/etf.py email-preview
```

### V93 DATA_LAG/Lrets=0 可读性 (2026-07-22)

#### 实现
1. today / live: **Lrets=0** 与 DATA_LAG 说明文案
2. `etf refresh` 结束后打印 asof/live 摘要
3. preview / status 下一步: DATA_LAG 提示
4. help / doctor / README / weekly: `data_asof` 入口

#### 验证
- today: Lrets=0 注 + DATA_LAG 注
- status 下一步含 DATA_LAG / check data_asof
- help 含 DATA_LAG 约定

#### 用法
```bash
python3 scripts/etf.py today
python3 scripts/etf.py check --checks data_asof
python3 scripts/etf.py refresh   # 末尾有 asof/live 摘要
```

### V94 signal_live 带 asof/lag + live 页 (2026-07-22)

#### 实现
1. `signal_live` 增加 `market_asof` / `data_lag`
2. latest.txt 有效收益块: Lrets=0 + 行情截至
3. 面板 live KPI: `仅锚日 · DATA_LAG`; live.html 滞后横幅
4. healthcheck `live` 详情含 asof/lag

#### 验证
- signal_live: `market_asof=2026-07-21 data_lag=true days_live=0`
- live.html / index KPI 含 DATA_LAG · 仅锚日
- `check --checks live` OK 且带 market_asof

#### 用法
```bash
python3 scripts/etf.py live
python3 scripts/etf.py pages
# output/site/live.html · index.html KPI
```

### V95 status/doctor/email 对齐 asof (2026-07-22)

#### 实现
1. status / today 有效收益块: **行情截至 + DATA_LAG + Lrets=0**
2. doctor `latest_signal_live`: asof/DATA_LAG/Lrets0 细节
3. 邮件 live KPI 副行: `Lrets · asof · DATA_LAG`

#### 验证
- today/status 含 `行情截至: 2026-07-21 DATA_LAG`
- doctor WARN 含 asof + Lrets=0仅锚日
- email: `Lrets=0 · asof 2026-07-21 · DATA_LAG`

#### 用法
```bash
python3 scripts/etf.py today
python3 scripts/etf.py status
python3 scripts/etf.py doctor
python3 scripts/etf.py email-preview
```

### V96 summary/monitor/站点次级页 DATA_LAG (2026-07-22)

#### 实现
1. `shadow_summary` / `shadow_monitor` 文本尾注: **行情截至 + DATA_LAG**
2. 站点 monitor/summary/compare 页复用 `_lag_banner_html`
3. 口径: Lrets=0=仅锚日; DATA_LAG 时等行情再判 xs

#### 验证
- `etf summary` / `monitor` 含 `行情截至: 2026-07-21 DATA_LAG`
- site: monitor/summary/compare/live/index 均含 DATA_LAG

#### 用法
```bash
python3 scripts/etf.py summary
python3 scripts/etf.py monitor
python3 scripts/etf.py pages
```

### V97 today/alerts/site_meta DATA_LAG (2026-07-22)

#### 实现
1. `today.html` / `alerts.html` 加 DATA_LAG 横幅
2. `site_meta.json`: `market_asof` / `data_lag`
3. status `signal_live` 补齐 asof/lag 字段
4. doctor `site_meta` 展示 asof/lag (lag 时 WARN)

#### 验证
- site_meta: `asof=2026-07-21 lag=True`
- today/alerts 含 DATA_LAG
- status signal_live 含 market_asof/data_lag
- doctor PASS + site_meta DATA_LAG WARN

#### 用法
```bash
python3 scripts/etf.py pages
python3 scripts/etf.py doctor
# output/site/site_meta.json · today.html · alerts.html
```

### V98 pipeline_last asof + 邮件主题 DATA_LAG (2026-07-22)

#### 实现
1. `pipeline_last.json` 记录 `data_asof` / `data_lag`
2. pipeline 日志: DATA_LAG 与 DONE 行带 asof/lag
3. 邮件主题附加 **DATA_LAG** (与 THIN 并列)
4. status / doctor 读 pipeline_last asof/lag

#### 验证
- pipeline_last: `asof=2026-07-21 lag=True`
- 邮件主题: `... THIN DATA_LAG`
- status: `pipeline 上次 ... asof=2026-07-21 lag=True`
- doctor: `pipeline_last ... DATA_LAG` WARN

#### 用法
```bash
python3 scripts/etf.py refresh
python3 scripts/etf.py status
python3 scripts/etf.py email-preview
# 主题扫一眼可见 DATA_LAG, 勿误读 xs=0
```

### V99 etf asof 取证 + site/pipeline 对齐 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py asof` 一页取证: 行情截至/DATA_LAG/live/pipeline/site
2. `site_meta`: `pipeline_asof` / `pipeline_lag`
3. healthcheck `pipeline` 详情含 data_asof/lag
4. GHA 打印 pipeline_last asof/lag
5. help / doctor / status / README 入口

#### 验证
- `etf asof` 输出 DATA_LAG + live + pipeline_last
- site_meta 含 pipeline_asof/lag

#### 用法
```bash
python3 scripts/etf.py asof
python3 scripts/etf.py pages
# 行情未到时先 asof, 再决定是否 refresh
```

### V100 DATA_LAG vs 过旧 决策树 (2026-07-22)

#### 实现
1. status/today/doctor 下一步决策树:
   - latest 过旧 → `refresh` 优先
   - 仅 DATA_LAG (信号新鲜) → `asof` 优先, 勿空转 refresh
2. `etf asof` 落盘 `output/asof.txt` + `risk_audit/asof.json`
3. 面板/邮件 DATA_LAG 横幅带 `etf asof` 命令
4. asof 下一步按 lag/stale 分支

#### 验证
- DATA_LAG 非 stale: today/status/doctor 优先 asof
- asof 产物存在
- index.html / email 含 etf.py asof

#### 用法
```bash
python3 scripts/etf.py asof
python3 scripts/etf.py today
# 仅 lag → asof; 过旧 → refresh
```

### V101 asof 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `asof` (today 后 / email 前)
2. daily 默认 steps: `...,today,asof,email,pages`
3. healthcheck `asof` 检查 + pipeline 产物要求 asof
4. doctor `asof_artifacts`
5. pages 复制 `asof.txt/json`, site_meta `has_asof`
6. GHA 上传/打印 asof 产物

#### 验证
- `etf asof` / `check --checks asof` / doctor asof_artifacts
- pipeline `--steps asof` 落盘
- site 含 asof 文件

#### 用法
```bash
python3 scripts/etf.py asof
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks asof
# 默认日更已含 asof 步骤
```

### V102 etf yield + asof 全路径展示 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py yield` 有效收益一页 (live%/xs%/THIN/Lrets) + 落盘
2. status 嵌入「行情取证 (asof)」摘要
3. pages: `asof.html` / `yield.html` + 导航「取证」「收益」
4. email `--append-asof`; pipeline/email-preview 默认附加
5. doctor site_meta `has_asof`

#### 验证
- yield 产物 + site 页
- status 含 asof 段
- email dry-print 含 asof

#### 用法
```bash
python3 scripts/etf.py yield
python3 scripts/etf.py asof
python3 scripts/etf.py status
python3 scripts/send_email.py --dry-print --append-asof
```

### V103 yield 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `yield` (asof 后 / email 前)
2. daily 默认: `...,today,asof,yield,email,pages`
3. healthcheck `yield` + pipeline 要求 yield 产物
4. doctor `yield_artifacts` + site_meta has_yield
5. status 嵌入「有效收益产物 (yield)」
6. email `--append-yield`; GHA 上传/打印 yield

#### 验证
- pipeline `--steps asof,yield` OK
- check --checks yield / doctor yield_artifacts
- email dry-print 含 yield 正文

#### 用法
```bash
python3 scripts/etf.py yield
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks yield
```

### V104 日更收口 YIELD + etf open (2026-07-22)

#### 实现
1. pipeline DONE 打印 YIELD live/xs/Lrets/asof
2. `etf daily` 收口摘要 (动作+live/xs)
3. `etf open [target] [--launch]` 关键产物路径
4. dashboard 首屏链到 yield/asof 页
5. refresh/today 下一步指向 yield/open

#### 验证
- pipeline --steps yield 收口含 YIELD
- etf open 列出路径
- index 含 yield.html / asof.html 链接

#### 用法
```bash
python3 scripts/etf.py yield
python3 scripts/etf.py open
python3 scripts/etf.py open --launch site
python3 scripts/etf.py daily --dry-run
```

### V105 brief 三合一 + pipeline.yield 取证 (2026-07-22)

#### 实现
1. `pipeline_last.json` 写入 `yield` 字段 (live/xs/Lrets/asof)
2. `python3 scripts/etf.py brief` 三合一速览 (yield+asof+today) + 落盘
3. doctor/status/preview/dashboard 入口含 brief/open
4. doctor pipeline_last 展示 live/xs
5. GHA 打印 brief + pipeline.yield

#### 验证
- brief 产物 + pipeline.yield 非空
- doctor/status 含 brief/open 下一步

#### 用法
```bash
python3 scripts/etf.py brief
python3 scripts/etf.py open --launch site
python3 scripts/etf.py daily --dry-run
```

### V106 brief 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `brief` (yield 后 / email 前)
2. daily 默认: `...,asof,yield,brief,email,pages`
3. healthcheck `brief` + pipeline 要求 brief 产物
4. doctor `brief_artifacts`
5. pages `brief.html` + 导航「速览」
6. email `--append-brief`; GHA 上传 brief
7. status 嵌入三合一摘要

#### 验证
- pipeline `--steps brief` OK
- check --checks brief / doctor brief_artifacts
- site brief.html

#### 用法
```bash
python3 scripts/etf.py brief
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks brief
```

### V107 pipeline.brief + etf data (2026-07-22)

#### 实现
1. `pipeline_last.json` 写 `brief` 取证字段
2. `python3 scripts/etf.py data` 行情状态/决策 (+ 可选 --fail-on-lag)
3. doctor site_meta has_brief; pipeline 展示 brief
4. weekly/daily GHA 打印 data/brief/pipeline.yield|brief
5. status/dashboard/daily 下一步含 data

#### 验证
- pipeline 后 pipeline_last.brief 非空
- etf data 决策=wait_data (DATA_LAG)
- doctor/status 含 data 入口

#### 用法
```bash
python3 scripts/etf.py data
python3 scripts/etf.py brief
python3 scripts/etf.py data --fail-on-lag  # 脚本门控退出码 3
```

### V108 data 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `data` (brief 后 / email 前) + `pipeline_last.data_status`
2. daily 默认: `...,brief,data,email,pages`
3. healthcheck `data_status`; pipeline 要求 data 产物
4. doctor `data_status_artifacts`; DATA_LAG 优先 `etf data`
5. pages `data.html` + 导航「行情」
6. email `--append-data`; status 嵌入行情状态
7. GHA steps 含 data

#### 验证
- pipeline `--steps data` + decision 打印
- check --checks data_status
- doctor priority 指向 data

#### 用法
```bash
python3 scripts/etf.py data
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks data_status
```

### V109 etf next + refresh 防空转 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py next` 唯一推荐下一步 (+ next.txt/json, --exit-code)
2. `refresh` 在 DATA_LAG 且信号未过旧时默认拦截 (exit 3), `--force` 可强制
3. 邮件主题增加 WAIT_DATA; 正文 lag 指向 next|data
4. dashboard 首屏 decision badge (WAIT_DATA/STALE/DATA_OK)
5. doctor/status/GHA/daily 对齐 next

#### 验证
- next decision=wait_data
- refresh 无 --force 被拦截
- email subject 含 WAIT_DATA

#### 用法
```bash
python3 scripts/etf.py next
python3 scripts/etf.py refresh          # DATA_LAG 时拦截
python3 scripts/etf.py refresh --force  # 强制
python3 scripts/etf.py next --exit-code
```

### V110 next 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `next` (data 后 / email 前, `--no-refresh`) + `pipeline_last.next`
2. daily 默认: `...,data,next,email,pages`
3. healthcheck `next`; pipeline 要求 next 产物
4. doctor `next_artifacts`
5. pages `next.html` + 导航「下一步」
6. email `--append-next`; status 嵌入 next
7. GHA steps 含 next

#### 验证
- pipeline `--steps next` OK + NEXT decision
- check --checks next
- site next.html

#### 用法
```bash
python3 scripts/etf.py next
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks next
```

### V111 etf pull 强刷行情 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py pull` force_refresh 基准/池缓存并复检 asof
2. 落盘 `pull.txt` / `risk_audit/pull.json` (before/after asof, advanced)
3. `next` 在 DATA_LAG 时推荐 `pull` (非空转 refresh)
4. doctor/status/dashboard/GHA 对齐 pull
5. 选项: `--bench-only` `--follow` `--fail-if-lag` `--fail-if-not-advanced`

#### 验证
- pull 可跑通; 源站未出当日K 时 advanced=false 属预期
- next 推荐含 pull

#### 用法
```bash
python3 scripts/etf.py pull --bench-only
python3 scripts/etf.py pull --follow
python3 scripts/etf.py next
```

### V112 pull 接入日更前段 (2026-07-22)

#### 实现
1. pipeline 步骤 `pull` (warmup 后 / signal 前; 默认 `--bench-only`)
2. daily 默认: `pull,signal,...,data,next,email,pages`
3. `pipeline_last.pull` 取证 + DONE 打印 advanced
4. healthcheck `pull` + pipeline 要求 pull 产物
5. doctor `pull_artifacts`
6. pages `pull.html`; status 嵌入 pull; email `--append-pull`
7. `etf next --pull` 决策前可先强刷
8. GHA steps 以 pull 开头

#### 验证
- pipeline `--steps pull` OK
- check --checks pull
- next --pull 可跑

#### 用法
```bash
python3 scripts/etf.py pull --bench-only
python3 scripts/etf.py next --pull
python3 scripts/etf.py daily --dry-run
python3 scripts/run_pipeline.py --steps pull,signal --pull-full
```

### V113 etf wait-asof 轮询 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py wait-asof` 轮询 `pull` 直到 asof 推进/清除 DATA_LAG 或超时
2. 成功默认 follow: `live` + `next` + `brief`
3. 落盘 `wait_asof.txt` / `risk_audit/wait_asof.json`
4. next/data/refresh 文案指向 wait-asof
5. doctor/status/dashboard/email(NO_ADVANCE) 对齐

#### 验证
- wait-asof --timeout 5 可快速返回 (未推进 exit 3/5)
- next 推荐含 wait-asof

#### 用法
```bash
python3 scripts/etf.py wait-asof --timeout 600 --interval 60
python3 scripts/etf.py wait-asof --timeout 120 --no-follow
python3 scripts/etf.py next
```

### V114 etf go 一键闭环 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py go` : pull → data → next → (wait_data 时 wait-asof) → brief
2. `next --wait` / `--wait-timeout` / `--wait-interval`
3. 落盘 `go.txt` / `go.json`
4. pages `go.html` + `wait_asof.html`; status/doctor 对齐

#### 验证
- go --no-wait 快速跑通
- next --wait --wait-timeout 5 可触发 wait-asof

#### 用法
```bash
python3 scripts/etf.py go --timeout 600
python3 scripts/etf.py go --no-wait
python3 scripts/etf.py next --wait --wait-timeout 300
```

### V115 go 接入日更快照 (2026-07-22)

#### 实现
1. pipeline 步骤 `go` (next 后; 默认 `--no-wait --no-pull` 快照, 不阻塞)
2. daily 默认: `...,next,go,email,pages`
3. `pipeline_last.go` + DONE 打印 GO decision
4. healthcheck `go`; pipeline 要求 go 产物
5. email `--append-go`; GHA 打印/上传 go
6. daily 摘要下一步指向 `etf go`

#### 验证
- pipeline `--steps go` OK
- check --checks go
- pipeline_last.go 有 decision

#### 用法
```bash
python3 scripts/etf.py go --no-wait
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks go
# 真要等行情推进:
python3 scripts/etf.py go --timeout 600
```

### V116 asof推进重跑signal + ready 可判性 (2026-07-22)

#### 实现
1. `wait-asof` 成功 follow: `signal --dry-run --shadow-exec` → live → yield → data → next → brief
2. `python3 scripts/etf.py ready` 有效收益可判性 (READY/THIN/WAIT_DATA/NOT_READY)
3. go 收口跑 ready; go ok 路径补 yield
4. doctor/status/pages 对齐 ready

#### 验证
- ready 在 DATA_LAG+THIN 给出 NOT_READY/WAIT_DATA
- follow 链路含 run_signal.py

#### 用法
```bash
python3 scripts/etf.py ready
python3 scripts/etf.py go --timeout 600
# asof 推进后会自动 signal+live+yield, 避免 latest 仍旧
```

### V117 ready 接入日更闭环 (2026-07-22)

#### 实现
1. pipeline 步骤 `ready` (go 后; 退出码 1/3/4 不失败)
2. daily 默认: `...,go,ready,email,pages`
3. `pipeline_last.ready` + DONE 打印 READY level
4. healthcheck `ready`; doctor `ready_artifacts`
5. email `--append-ready` + 主题标签 NOT_READY/THIN/READY
6. GHA 打印/上传 ready

#### 验证
- pipeline `--steps ready` OK
- check --checks ready
- email subject 含 NOT_READY

#### 用法
```bash
python3 scripts/etf.py ready
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks ready
```

### V118 digest 摘要 + READY 首屏徽章 (2026-07-22)

#### 实现
1. `python3 scripts/etf.py digest` 人读一页: 可判性+live/xs+decision+推荐
2. dashboard 首屏 READY/NOT_READY/THIN 徽章 (ready.json)
3. pages `digest.html`; doctor/status 入口
4. 落盘 digest.txt/json

#### 验证
- digest 在 DATA_LAG 显示 NOT_READY + 推荐 go/wait-asof
- index 含 NOT_READY/READY badge

#### 用法
```bash
python3 scripts/etf.py digest
python3 scripts/etf.py digest --no-refresh
python3 scripts/etf.py ready
```

### V119 digest 接入日更收口 (2026-07-22)

#### 实现
1. pipeline 步骤 `digest` (ready 后; `--no-refresh`; 退出码不失败)
2. daily 默认: `...,ready,digest,email,pages`
3. `pipeline_last.digest` + DONE 打印 DIGEST
4. healthcheck `digest`; doctor `digest_artifacts`
5. email `--append-digest`; status 嵌入 digest
6. GHA 打印/上传 digest

#### 验证
- pipeline `--steps digest` OK
- check --checks digest
- digest 为人读有效收益结论入口

#### 用法
```bash
python3 scripts/etf.py digest
python3 scripts/etf.py daily --dry-run
python3 scripts/etf.py check --checks digest
```

### V120 ./etf 零配置入口 + 面板 DIGEST 首屏 (2026-07-22)

#### 实现
1. 项目根 `./etf` 包装: 自动 PYTHONPATH; 无参数默认 `digest`
2. 可选 `~/.local/bin/etf` 软链
3. `scripts/etf.py` 无子命令时默认 digest
4. dashboard 首屏 DIGEST 行: 可判性/live/xs/推荐
5. site_meta `has_digest`; doctor 展示 has_digest

#### 验证
- `./etf` 输出 DIGEST
- index 含 DIGEST 卡片与 NOT_READY
- `python3 scripts/etf.py` 无参 = digest

#### 用法
```bash
./etf
./etf go --timeout 600
./etf ready
./etf digest --no-refresh
```

### V121 对外命令统一 ./etf (2026-07-22)

#### 实现
1. 用户可见推荐/下一步/拦截文案统一为 `./etf ...`
2. doctor/status/email/pages 对齐
3. next/data/ready/digest/refresh 提示统一
4. pipeline DONE 打印 digest 正文前几行
5. 内部 subprocess 仍走 `scripts/etf.py` (不变)

#### 验证
- `./etf` / `./etf digest` 正常
- doctor 优先项含 `./etf digest`
- 无破坏内部 PY+SCRIPTS 调用

#### 用法
```bash
./etf
./etf go --timeout 600
./etf wait-asof
./etf ready
```

### V122 面板入口 + 产物推荐统一 (2026-07-22)

#### 实现
1. dashboard muted 精简为 `./etf digest|go|ready`
2. DIGEST 卡片推荐路径规范化 (`python3 scripts/...` → `./etf`)
3. status 首屏可判性 level + 推荐
4. 残留 help 文案 `./etf compare`
5. 刷新 go/digest 产物推荐

#### 验证
- index muted 含 ./etf digest / go
- ./etf next 推荐 ./etf
- go.json recommend 为 ./etf ...

#### 用法
```bash
./etf
./etf go --timeout 600
./etf open --launch site
```

### V123 etf eod + 距可判天数 (2026-07-22)

#### 实现
1. `./etf eod` 收盘闭环: wait-asof → (推进则 signal/live/yield) → daily --dry-run → ready/digest
2. `ready`/`digest` 增加 `days_to_ready` (距 Lrets≥5 的交易日数)
3. doctor/status/pages/README 对齐 eod
4. 落盘 `eod.txt` / `eod.json`

#### 验证
- `./etf eod --no-wait` 可快照跑通
- ready 显示距可判天数

#### 用法
```bash
./etf eod --timeout 1800
./etf eod --no-wait          # 不轮询, 只日更快照
./etf ready                  # 看 days_to_ready
```

### V124 eod 产物面 + days_to_ready 展示 (2026-07-22)

#### 实现
1. digest/eod 产物字段补齐 `days_to_ready`
2. dashboard DIGEST 卡片显示距可判天数 + eod 链接
3. pages `eod.html`; status 嵌入 eod; doctor `eod_artifacts`
4. email `--append-eod`; open/GHA 对齐
5. **不把 eod 放入默认 daily** (避免重等待/递归)

#### 验证
- `./etf eod --no-wait` 产物含 days_to_ready
- digest 含距可判; index DIGEST 显示距可判
- doctor 识别 eod_artifacts

#### 用法
```bash
./etf eod --no-wait
./etf digest
./etf open --launch site
```

### V125 progress 可判性轨迹 + 邮件 DTR (2026-07-22)

#### 实现
1. `_append_progress` + `progress.jsonl` / `progress_latest.json` / `progress.txt`
2. `ready` / `eod` 自动写入轨迹 (同日同 source 去重)
3. `./etf progress` 查看 Lrets→READY 轨迹
4. status/doctor/pages 对齐; 邮件主题 `DTR{n}`
5. 用于真实有效收益: 量化“还要几天可判”

#### 验证
- `./etf ready` 后 progress.jsonl 有记录
- `./etf progress` 显示 dtr/Lrets
- email subject 含 DTR5 (样本不足时)

#### 用法
```bash
./etf ready
./etf progress
./etf eod --no-wait
```

### V126 progress 接入日更收口 (2026-07-22)

#### 实现
1. pipeline 步骤 `progress` (digest 后; `--no-refresh`)
2. daily 默认: `...,digest,progress,email,pages`
3. `pipeline_last.progress` + DONE 打印 PROGRESS
4. healthcheck `progress`; digest 也写入轨迹
5. email `--append-progress`; GHA 打印/上传 progress

#### 验证
- pipeline `--steps progress` OK
- check --checks progress
- daily 收口含 progress 轨迹

#### 用法
```bash
./etf progress
./etf daily --dry-run
./etf check --checks progress
```

### V127 asof 推进事件轨迹 + progress JSON (2026-07-22)

#### 实现
1. `_snapshot_progress(source)` 从 ready.json 写轨迹
2. `wait-asof` 成功 follow: signal→…→brief→**ready**→progress(src=wait_asof)
3. `go` 收口 snapshot src=go (与 ready 并存, 事件可辨)
4. `./etf progress --json` 脚本取证
5. `./etf open` 覆盖 ready/digest/progress/eod/go 与对应 html

#### 验证
- `./etf go --no-wait --no-pull` 后 progress 含 src=go
- `./etf progress --json` 有 trend/latest
- `./etf open progress`

#### 用法
```bash
./etf go --no-wait --no-pull
./etf progress --json
./etf open progress,ready
./etf wait-asof --timeout 600
```

### V128 pull 推进闭环 + ETA 可读 (2026-07-22)

#### 实现
1. `pull` asof 推进默认 follow: signal dry+shadow → live → yield → data → next → ready → progress(src=pull)
2. 未推进写 `pull_noadv` 事件 (同日去重); `--no-follow` 可关
3. ready/progress/status 增加 **ETA** (约再 N 个交易日 READY)
4. status 轨迹显示 Δ Lrets 与 src
5. digest 写轨迹后提示 `./etf progress`

#### 验证
- `./etf pull --bench-only` 落盘 + progress 事件
- `./etf ready` 含 ETA 行
- status 含 Δ/ETA

#### 用法
```bash
./etf pull --bench-only
./etf progress
./etf ready
./etf status
```

### V129 ETA 铺到 yield/brief/next/data/digest/eod (2026-07-22)

#### 实现
1. `_load_ready_eta` / `_days_to_ready_from_live` 复用
2. yield/brief/next/data 打印 **距可判 + ETA**, 产物 JSON 带 `days_to_ready`/`eta_note`
3. digest 显示 ETA; 未 READY 时推荐附加 `./etf progress`
4. eod 结论行 ETA + progress 推荐
5. data/next 在 DATA_LAG 时辅读 progress/ready

#### 验证
- `./etf yield|brief|data|next|digest` 均含 ETA/dtr
- JSON 字段可被脚本读取

#### 用法
```bash
./etf yield
./etf brief
./etf next --no-refresh
./etf digest --no-refresh
```

### V130 ETA 上屏: pages/pipeline/today/asof/status (2026-07-22)

#### 实现
1. 面板 DIGEST/PROGRESS 卡显示 ETA; yield/ready 页 muted 带 dtr/ETA
2. pipeline DONE 打印 `ETA dtr=… → ./etf progress`
3. `today` / `asof` 输出距可判+ETA+progress 提示
4. status 可判性首屏增加 ETA 行

#### 验证
- `./etf today|asof` 含 ETA
- `python3 scripts/build_pages.py` 面板有 ETA
- pipeline DONE 有 ETA 行

#### 用法
```bash
./etf today
./etf asof
./etf status
./etf pages
```

### V131 邮件 ETA 正文 + site_meta/pipeline_last (2026-07-22)

#### 实现
1. 邮件 plain/html 首屏 **可判/ETA** 块 (`_eta_summary_line`)
2. `site_meta.json`: `days_to_ready` / `eta_note` / `ready_level`
3. `pipeline_last.json` 顶层 promote dtr/eta/level
4. `asof.txt` 落盘含 dtr/eta

#### 验证
- `send_email --dry-print` 含 可判/ETA
- site_meta 含 days_to_ready
- pipeline_last 顶层 dtr

#### 用法
```bash
python3 scripts/send_email.py --dry-print --append-progress
./etf asof
./etf pages
cat output/site/site_meta.json | head
```

### V132 etf pulse 一键脉搏 (2026-07-22)

#### 实现
1. `./etf pulse` = data + ready + ETA + progress 摘要 (exit 0/3/4)
2. 落盘 `pulse.txt` / `pulse.json`; progress 事件 src=pulse
3. go/pull 收口显示距可判/ETA; 推荐辅读 pulse
4. pages pulse.html; GHA 打印 ETA/pulse
5. 人用最短读口: 看真实有效收益是否可判

#### 验证
- `./etf pulse` 含 level/dtr/ETA/推荐
- pulse.json 字段齐全
- go --no-wait 显示 ETA

#### 用法
```bash
./etf pulse
./etf pulse --no-refresh
./etf pulse --json
./etf go --no-wait --no-pull
```

### V133 pulse 接入日更收口 (2026-07-22)

#### 实现
1. pipeline/daily 步骤 `pulse` (progress 后; `--no-refresh`)
2. `pipeline_last.pulse` + DONE 打印 PULSE
3. healthcheck `pulse`; doctor pulse_artifacts
4. email `--append-pulse`; status 脉搏块; 面板 PULSE 卡
5. `./etf wait` 别名 → wait-asof

#### 验证
- pipeline `--steps pulse` OK
- check --checks pulse
- email dry-print 含 PULSE

#### 用法
```bash
./etf pulse
./etf wait --timeout 600
./etf daily --dry-run
./etf check --checks pulse
```

### V134 推荐统一指向 pulse (2026-07-22)

#### 实现
1. next/data/digest/ready/yield/brief 推荐与辅读优先 `./etf pulse`
2. live/compare/refresh 收口提示 pulse
3. doctor/status 首屏优先 pulse
4. 人用决策树: **pulse 看可判** → wait-asof/go 推进 → 再读 live%/xs%

#### 验证
- `./etf next --no-refresh` 推荐含 pulse
- `./etf data` 辅读 pulse
- doctor 优先含 pulse

#### 用法
```bash
./etf pulse
./etf next --no-refresh
./etf data
./etf wait --timeout 600
```

### V135 裸 ./etf=pulse + quiet (2026-07-22)

#### 实现
1. 裸 `./etf` 默认 `pulse` (最短: 能否读真实有效收益)
2. `pulse --quiet` 单行摘要; `--quiet --json` 纯 JSON
3. pulse 打印 exit 码含义; open `pulse-html`
4. pipeline DONE 下一步 → pulse; `wait` 别名写入 help

#### 验证
- `./etf` ≡ `./etf pulse`
- `./etf pulse --quiet` 单行
- `./etf pulse --quiet --json` 可 parse

#### 用法
```bash
./etf
./etf pulse --quiet
./etf pulse --quiet --json
./etf wait --timeout 600
```

### V136 pipeline quiet + 收口统一 pulse (2026-07-22)

#### 实现
1. pipeline `pulse --no-refresh --quiet` 降日志噪音
2. wait-asof / eod 结束提示 `./etf pulse --quiet`
3. 邮件主题非 READY 时加 **PULSE** 标签
4. doctor/pages/status/README: 裸 `./etf`=pulse, cron 用 `--quiet`

#### 验证
- pipeline --steps pulse 日志短
- email subject 含 PULSE DTR5
- wait-asof/eod 有下一步 pulse

#### 用法
```bash
./etf
./etf pulse --quiet
./etf eod --timeout 1800
./etf wait --timeout 600
```

### V137 pulse 决策树机器字段 (2026-07-22)

#### 实现
1. pulse.json 增加: `next_action` / `why` / `exit_code` / `readable_yield`
2. next_action 枚举: refresh | wait_asof | accumulate | read_yield | doctor
3. 精简 next/digest 推荐为单一短命令
4. pipeline/healthcheck/GHA 对齐 quiet + action 打印

#### 验证
- `./etf pulse --quiet --json` 含 next_action/exit_code
- next 推荐短: `./etf wait --timeout 600`
- readable_yield=false 当 DATA_LAG

#### 用法
```bash
./etf pulse --quiet --json | jq .next_action,.readable_yield,.exit_code
./etf wait --timeout 600
```

### V138 etf do + readable_yield 门控 (2026-07-22)

#### 实现
1. `./etf do` / `act` 执行 pulse.next_action
2. yield 打印 readable_yield 门控
3. status/pages/email/pipeline_last 展示 next_action
4. 落盘 do.txt / do.json

#### 验证
- `./etf do --dry-run` → wait_asof
- `./etf yield` 门控 false
- site_meta.next_action

#### 用法
```bash
./etf do --dry-run
./etf do --timeout 600
./etf yield
```











































































































---

## 已验证有效

1. **趋势过滤(大盘MA20以下空仓)** — 最关键, V1→V2从-43%到+84%
2. **纯动量因子** — 比多因子更有效
3. **过热过滤(20日>30%不追)** — 避免接飞刀
4. **迟滞过滤(新候选高15-20%才换)** — 降低换手
5. **持1只利于主题年进攻; 持2只利于全天候/结构年** — V11 修正: 全天候看 beat_years 时 top2 更稳
6. **双均线过滤(MA10+MA20)** — 进一步降回撤

## 已验证无效/弱

1. 风险调整动量(Sharpe) — 稀释进攻性
2. 加速度动量 — 追高
3. 最大持仓期限制 — 几乎无效果
4. 移动止损 — 帮助有限
5. 综合多因子(7个等权) — 不如简单动量
6. 同行业ETF内部轮动 — 无意义,高度相关

### V8 大规模探索(112组实验)

| 实验 | 发现 | 最佳 |
|------|------|------|
| A. lookback窗口 | **长窗口远优于短窗口** | lb=60, 夏普2.6 |
| B. 因子组合 | **效率动量+趋势组合最强** | 纯效率夏普4.1, 趋势效率夏普3.3 |
| C. 绝对动量/市场宽度 | 宽度20-40%有正面效果 | bm=0.4, 夏普2.4 |
| D. 双均线+波动率仓位 | 双均线有效, 波动率仓位正面 | dm=True, 夏普1.7-1.8 |
| E. 移动止损 | 对跨行业池帮助有限 | 不如固定止损 |
| F. 多周期一致性+风控 | mtf+双均线组合稳定 | 夏普2.4 |
| G. 持仓数×风控 | **持2只夏普3.5, 持3只回撤-11.6%** | tn=2最优 |
| H. 止损阈值 | -5%较好(+80%), 差异不大 | -5%略优 |
| I. 迟滞阈值 | **高迟滞(40-50%)更好** | 50%夏普1.9, 说明纪律性重要 |
| J. 调仓频率 | **15天夏普4.5, 15天+双均线回撤-14.4%** | 低频大幅优于高频 |
| K. 最小持仓期 | **7-15天夏普2.7-2.8** | 避免过早换仓 |
| L. 持仓数 | tn=2回撤-14%, tn=5回撤-12.2% | 2-3只平衡最优 |

### V8 全局TOP5

| 排名 | 配置 | 收益 | 年化 | 回撤 | 夏普 |
|:---:|------|:----:|:----:|:----:|:----:|
| 🥇 | **15天调仓+双均线** | **+139.7%** | **+65.1%** | **-14.4%** | **4.5** |
| 🥈 | **纯效率因子** | **+181.1%** | **+80.9%** | **-19.8%** | **4.1** |
| 🥉 | 15天调仓(无双均) | +148.7% | +68.6% | -18.6% | 3.7 |
| 4 | 持2只 | +100.7% | +49.1% | -14.0% | 3.5 |
| 5 | 趋势效率因子 | +145.8% | +67.5% | -20.7% | 3.3 |

### V8 核心结论

1. **长窗口+低频=最佳**: lookback 60日 + 调仓15天 + 双均线 = 夏普4.5
2. **效率因子被低估了**: 纯效率(净移动/总波动)夏普4.1, 之前没用过
3. **持2只比1只好**: 35只ETF池中, 持2只大幅降回撤
4. **高迟滞有帮助**: 40-50%迟滞比10-15%更好——管住手
5. **移动止损几乎无用**: 跨行业ETF池中, 趋势过滤比移动止损更有效
6. **市场宽度有效**: 20-40%ETF上涨才入场, 可以提高稳定性

### V9 组合赢家 + 过拟合检验 (112组)

样本: 2024-06-25 ~ 2026-07-17 (501天), 主池35只; 成长/价值各20只

| 配置 | 全样本收益 | 回撤 | 夏普 | 切分正收益 | 稳健分 | 备注 |
|------|:----:|:----:|:----:|:----:|:----:|------|
| **C01.效+15d** | **+163.9%** | **-12.2%** | **6.1** | **5/5** | **7.13** | 🏆 最优稳健 |
| C13.动量+15d+双均+持2+h40 | +97.4% | -9.9% | 4.8 | 5/5 | 6.48 | 低回撤稳健 |
| V8.J15双均 | +139.7% | -14.4% | 4.5 | 5/5 | 6.48 | V8冠军复现 |
| C15.平衡+15d+双均+持2 | +103.0% | -10.7% | 4.7 | 5/5 | 6.24 | 平衡因子 |
| C36.平衡+15d+持3 | +84.3% | -8.6% | 4.9 | 5/5 | 6.23 | 最低回撤 |
| V8.B10纯效率(周调) | +181.1% | -19.8% | 4.1 | **4/5** | 5.19 | 前半段亏, 过拟合风险 |

#### C01 切分明细 (效+15d)
- 前半: +7.6% 夏普1.0 | 后半: +64.5% 夏普9.3 | 中段: +64.3% 夏普7.8
- 成长池: +141.8% 夏普5.4 | 价值池: +76.7% 夏普3.3
- 全样本交易仅48次 (低频)

#### V9 关键结论
1. **效率因子 + 15天调仓 = 最强稳健组合** (C01), 夏普6.1, 5/5正收益
2. **周频纯效率(B10)前半段亏损** → 全样本高收益有过拟合嫌疑, 不宜直接上线
3. **双均线略降收益但降回撤**, 持2/3进一步降回撤 (C13/C36 回撤-8~-10%)
4. **市场宽度过滤(bm≥0.3)过度保守**, 收益腰斩, 暂不采用
5. **叠加参数过多(全能A-E)反而变差** → 保持简单
6. **推荐生产配置**:
   - 主选: `w={eff:0.6, mtf:0.4}, rb=15, top_n=1, dual_ma=False, hyst=0.20, min_hold=5, stop=-0.08`
   - 保守备选: `w={m20:0.5,m5:0.3,eff:0.2}, rb=15, dual_ma=True, top_n=2, hyst=0.40, min_hold=10` (C13)

结果文件:
- `/home/abc/桌面/ETF轮动信号/v9_results.csv`
- `/home/abc/桌面/ETF轮动信号/v9_robust.csv`
- `/home/abc/桌面/ETF轮动信号/v9_run.log`

### V10 抗过拟合检验

| 检验 | 方法 | 关键发现 |
|------|------|----------|
| Walk-forward | 4折 train选参→test | 选参OOS均收益+3.6%, 正折3/4 |
| 固定配置OOS | 各配置在4个test窗 | C01: +3.4%/折, 3/4正; B10: +10.5%但前折差 |
| 成本敏感 | 万0.5 → 万5+滑点万10 | **C01最稳**: 极端仅-14.5pp; 周频B10 -26.7pp |
| 参数邻域 | rb/hyst/mh/权重扰动 | **C01: 21/21全正, CV=0.21**; C13: 17/17, CV=0.29 |

#### 如何解决过拟合 (已落地)

1. **样本外优先**: 不以全样本夏普定生死, 看 walk-forward / 时间切分 / 换池
2. **简单优先**: 拒绝「全能堆参数」; C01 仅 eff+mtf+15d
3. **成本加压**: 低频策略对滑点不敏感 → 更可信
4. **邻域平坦**: 参数小扰动不崩溃 → 非刀锋最优
5. **固定后冻结**: 生产配置至少跑3个月; 连续2个调仓周期失效→降仓, 不立刻重优化
6. **警惕短窗年化**: 单折OOS夏普可被极短区间放大, 看正折比例+最差折

#### 生产上线
- `etf_signal_v2.py` → **C01_效15d** (eff0.6+mtf0.4, rb=15, top_n=1, MA20趋势, 止损-8%, 迟滞20%, 最小持仓5日)
- 佣金对齐回测: 万0.5
- 调仓窗口: `last_rebalance` + 15自然日

结果: `v10_results.csv` / `v10_run.log`

### 更长样本如何获取 (C01 已冻结, 备查)

当前 `stock-api get-klines` **支持长历史**, 之前回测只用了 `count=500`(~2年)。

```bash
# 日线最长实测
npx -y stock-api get-klines SH510300 --period day --count 2500 --adjust qfq
# → 3436根, 2012-05-28 ~ 2026-07-17

# 行业ETF受上市日限制, 不是接口上限
# SH512760 芯片: 2019-06 起 (~1722根)
# SH515120 创新药: 2021-01 起 (~1341根)
# SH588000 科创50: 2020-11 起
```

| 要点 | 说明 |
|------|------|
| 接口 | `count` 提到 2000~3000 + `--adjust qfq`(前复权) |
| 真实瓶颈 | **ETF上市日**, 不是API; 池子交集常从最晚上市的那只开始 |
| 做长样本WF | 应用「共同交易日交集」或「允许个股缺失、用可得子集」 |
| 周线 | `period week --count 500` 可覆盖更久但信号频率变了 |
| 其它源 | ashare-mcp / 东财 / tushare(需token) 可补, 目前 stock-api 已够用 |

**C01 冻结纪律**: 生产信号不因长样本结果改参; 长样本仅用于事后验证报告。

### 体验增强 (信号脚本 v2.1)

已做（优先于再搜参数）:
1. **今日动作卡片** — 一眼看要不要动手
2. **决策检查清单** — 大盘/调仓窗/止损/合格标 逐项 ✓✗
3. **调仓时钟** — 上次/已过/还需 X 日
4. **数据异常过滤** — 单日>|12%| 或 20日>|45%| 剔除坏点
5. **C13 影子对照** — 只展示不交易
6. **固定入口** — `latest.txt` + `latest.json`
7. **实盘备忘** — 按当前状态给出 1-2-3 步操作提示
8. **市场宽度** — 上涨ETF占比

未做（可选, 收益/成本权衡）:
- 推送(邮件/微信) — 需用户账号
- 更长历史 walk-forward — 数据源 count 上限约500
- 再搜参数 — 边际收益低, 过拟合风险升

---



## 详细迭代记录

### V1 基线
- 因子: mom20 25% + mom5 15% + vol 20% + vol_trend 20% + ma_dev 20%
- 结果: -43%, 回撤-61%
- 问题: 大盘下跌时满仓硬扛

### V2 趋势过滤
- 新增: 沪深300 < MA20 → 空仓; 止损冷却5天; 过热过滤
- 结果: +84%, 回撤-12%
- 结论: 趋势过滤是核心风控

### V3 因子权重
- 纯动量(50+30+20) 最优 +134%
- 低波主导 胜率最高但收益一般

### V4 参数网格(144组)
- 最优: 调仓7天/持1只/迟滞15%/止损-10% → +209% 夏普7.0
- 但池子含3只创新药高度相关 → 过拟合风险

### V5 过拟合检验
- 原池 +209%, 行业ETF池 -41%, 宽基 +43%
- 结论: 收益严重依赖池子构成

### V6 跨行业池
- 重建真正跨行业10赛道池
- 最优: 调仓5天/持1只/迟滞20% → +141% 夏普3.2
- 换池子仍正收益 → 通过基本稳健性检验

### V7 新因子
- 多周期一致性略胜纯动量
- 双均线过滤: 回撤-17.6%, 夏普2.7
- 风险调整动量/加速度动量表现差

---
